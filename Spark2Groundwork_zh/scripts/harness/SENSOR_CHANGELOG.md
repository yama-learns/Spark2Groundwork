# 感測器變更記錄

**⛔ 每一次感測器變更都要在這裡登錄觸發個案**（憲章 §7.1 第三關）。

**格式：** 編號｜日期｜一句話。內容須含：觸發個案、修了什麼、成對樣本在哪。

> 🔴 **本檔有兩套 `#n`，共用一個命名空間。**
> **前史條目無日期；維護期條目一律帶日期。**
> ⛔ **引用維護期條目時必須連日期一起寫**（例：`#12 ｜ 2026-08-20`）；
> **引用前史條目寫「前史 #n」。⛔ 只寫 `#1` 是有歧義的。**
> ⚠️ **本檔為只增不改寫，⛔ 因此不重編號**——**改的是引用方式，不是紀錄本身。**
> ⚠️ **前史條目為各版本各自的歷史，⛔ 兩版不必一致**（英文版多一則英文版特有的常數問題）。

---

## 前史（**無日期**）

## #0 ｜ 框架初始版本

**本套感測器移植自兩個實際運行的專案，每一支都對應真實個案：**

| 感測器 | 觸發個案 |
|---|---|
| `sensor_claim_ledger.py` | 評估報告的四組數值在原文中皆查無出處，且原文結論與轉述**方向相反** |
| ├ 錨點正規化 | 一筆錨點被 `identiﬁed` 的連字截斷成 20 字元殘句，當時誤診為「提取物的限制」 |
| └ 提取物雜湊 | 開放寫入語料庫時發現：**可寫入者若能改動提取物，證據鏈會變成循環** |
| `sensor_conjecture_ledger.py` | 猜想的反證條件欄長期為空，而該猜想仍被當成前提引用 |
| `sensor_self_certification.py` | 64 份檔案檔尾自述「所有數值皆經核實」，而同批檔案中查出捏造樣本數 |
| `sensor_governance_text.py` | 跨檔完全相同的句子 17 條；懸空章節引用 8 筆，**其中 4 筆自建案起即存在** |
| `sensor_scope_and_t0.py` | 一支檢查程式對著**錯誤的 repo** 回報 16 項違規，且語氣非常肯定 |
| `sensor_prompt_self_contained.py` | 某 prompt 的 §1 引述「不得寫『同上』」，**下半部寫了「Same as R3-1」** |

⚠️ **這是「無實測個案不建感測器」的唯一例外，理由寫在這裡：**
**這些個案發生在別的專案，不是你的。**
它們列在這裡是**為了讓你少走歪路，不是宣稱你已經免疫**。

⛔ **你新增的每一支感測器，仍須遵守「無實測個案不建」。**

---

## #1 ｜ 框架自身的 solo agent 實測（**11 項缺陷，全部由受測 agent 發現**）

**方法：** 派一個獨立 agent 在框架副本中執行完整流程（讀規則 → 拆解一段跨領域散文 →
填空 → 跑感測器 → 交封包），並額外回答四個回饋問題。

### 🔴 最嚴重：框架自己的模板，教人寫出一種會關掉框架自己感測器的寫法

`TEMPLATE_decompose.txt` 第 3 條原本建議：反證條件填不出來時「標記為『反證條件待填』」。

**實測：照做之後，`FALSIFICATION_EMPTY` 由 2 筆掉到 1 筆，退出碼仍為 0。**
原因：感測器的 `PLACEHOLDERS` 只認完全相符的字串，
**「反證條件待填」被當成已填。**

⚠️ **這是「靜默過濾」家族，而觸發它的是框架自己的模板。**

### 🔴 更深一層：獎懲方向是反的

受測 agent 的原話：

> 留「—」看起來像我做得比較少；而編一句「若表徵相似性分析達 r > .8 則本條被推翻」
> 讀起來完全合格，**沒有任何人、任何感測器分得出來那是裝飾品**。
> 它甚至會讓那一條從 WARN 變成無警。
> **這條規則的獎懲方向是反的：誠實填「—」會亮黃燈，唬爛不會。**

**修正：告警條件由「欄位為空」改為「尚未經人裁決」。**
新增 `反證條件裁決` 欄位，**填了字不會讓警示消失，只有人改成 `已裁決` 才會。**
⛔ AI 不得自行改該欄。
**檢查碼由 `FALSIFICATION_EMPTY` 更名為 `FALSIFICATION_UNADJUDICATED`。**
成對 fixture：`conj_filled_unadjudicated`（填了字仍須告警）＋ `conj_clean`（已裁決不誤報）。

### 其餘九項

| # | 缺陷 | 處置 |
|---|---|---|
| 1 | §2.1 標題寫「三級」而表格列四個 | 改為「查證等級標記」，不寫數字（R-16） |
| 2 | 「產出區」全框架無定義，而 §6.1 要求「只寫自己的產出區」 | 憲章 §6.1 補定義 |
| 3 | 憲章 §4.2 要求覆寫狀態文件，§6 又暗示不該 | 明文把狀態文件納入每個角色的產出區 |
| 4 | **無「指令抵觸 T0 時的當下處置」規則** | 憲章 §4.3 新增四選一處置 |
| 5 | 台帳 `<歸屬>` 欄無定義 | 補定義 |
| 6 | `HANDOFF` 的 `<角色>` 無取值列舉 | 補列舉（「必填欄位誘發捏造」家族的入口） |
| 7 | `MODEL_IDENTITY` → `HANDOFF §9` 懸空引用 | 已修（感測器抓到） |
| 8 | `Audit_Protocol` ↔ `HANDOFF` 跨檔重複長句 | 已改為引用（感測器抓到） |
| 9 | 權威型號欄位不在協定所寫的位置時無處置 | `MODEL_IDENTITY` §3.7 新增三階降級 |

### ⚠️ 第 4 項是本次最深的結構缺口

受測 agent 收到「把猜想寫進台帳」的指令，而**四份文件都寫著 AI 不得寫入台帳**。
它自己從豁免條款推導出處置方式並執行了，並登記了豁免。

> **一個 agent 靠自己推導出「我可以破例」，本身就是這套框架最該擔心的事。**

**框架能容忍破例，不能容忍沒有人知道破過例。**

### ✅ 兩項證明框架有效的觀察

1. **受測 agent 對七筆 WARN 逐筆判定，一筆都沒有為了讓畫面變綠而改文件。**
2. `Incident_Log` 的失效家族表起了作用——受測 agent 的原話：
   **「我是靠『靜默過濾』這個名字才認出模板與感測器的那個缺陷是什麼形狀的。」**

### ⛔ 本次測試無法驗證的事（**測試涵蓋範圍聲明**）

- **受測 agent 與框架作者是同一模型家族。** 跨模型對抗審計那一層**完全未受測**。
- 單輪測試。跨輪次的漂移（「索引當權威」「修一層漏另一層」「文件增生」三族）**未受測**。
- 未測外部工具情境、多角色情境、Windows 批次檔。

---

## 維護期（**一律帶日期**）

## #1 ｜ 2026-08-19 ｜ 提取工具：後端可辨識 ＋ manifest 自檢命中率

**觸發個案（三件，來自另一個實際運行的專案與本次維護）：**

1. **同一批 11 篇論文的兩份提取物互測，錨點嚴格命中率 62.5%、去空白標點後 95.7%**——
   33.2 個百分點純粹是空白與連字號差異。舊後端 pypdf 在部分 PDF 上遺失行尾空白產生黏字，
   而 `anchor_norm.py` 能壓縮空白卻**無法插入不存在的空白**。
   → **改提取器，不改比對器**（憲章 §7.3 第 1 步；`R-20`）。

2. **某競品工具在主後端失敗時靜默改用另一條提取路徑**，兩種提取物混在同一批而檔案裡看不出來。
   → 後端名稱寫進檔頭與 manifest；降級留下 `degraded_from` 紀錄。**降級可以，靜默不行。**

3. **`out_dir.mkdir()` 在「有沒有 PDF」的檢查之前**，於是一次誤跑就留下空的 `corpus_md/`，
   而 `sensor_claim_ledger.py` 對此報 INCOMPLETE——**整套 harness 從 0 變成 2，
   而 `git status` 看不到它**（git 不追蹤空目錄）。→ 調換順序。

**修了什麼：**

| 檔案 | 變更 |
|---|---|
| `tool_pdf_to_md.py` | PyMuPDF 主後端 ＋ pypdf 後備；後端登錄；孤兒檢查；`mkdir` 順序；**manifest 新增 `selfcheck_hit`／`selfcheck_n`／`anchors_spanning_pages`** |
| `tool_extract_compare.py` | **新增。** 嚴格／寬鬆／差額三欄——差額欄區分「正規化涵蓋不足」與「提取品質不良」 |

**成對樣本在哪：** ⚠️ **目前沒有。** 兩者是**工具，不是感測器**，
不在 `run_all_sensors.py` 套件內，故未依憲章 §7.1 建立 fixture。
⛔ **若日後把 `selfcheck_hit` 升格為感測器判準，須先補成對樣本。**

**自檢的實測（`[我實測]`，可重跑）：**

首版對一篇 9 頁論文得到 **93.3%**，兩筆未命中**全部是跨越 `<!-- page: N -->` 的句子**。
⚠️ 依憲章 §7.3 第 1 步複核後確認：**壞的是抽樣方式，不是提取物。**
→ 改為逐頁抽句；跨頁候選只回報數量（`anchors_spanning_pages`），不計入命中率。
**修正後同一篇為 100%（抽樣 30 句，跨頁候選 6 句）。**
→ 同一個發現同時產生了 `ledgers/Claim_Ledger.md` §1.1 第 5 條（**錨點不得跨頁**）。
⚠️ **（該節已於 v1.4.1 搬至 `governance/CLAIM_LEDGER_SPEC.md` §1.1。⛔ 本行保留原文，因為它記的是當時的事。）**

---

## #2 ｜ 2026-08-19 ｜ 自測：「不得誤報」那一半不再只驗退出碼

**觸發個案（他專案實測 → 本框架實查確認同樣成立）：**

`WARN` 級 finding **不改變退出碼**（`_common.py` 的 `emit`：只有 FAIL→1、INCOMPLETE→2）。
於是「不得誤報」測試若只斷言 `exit == 0`，**對任意多筆 WARN 級誤報一律印 ✅。**

**本框架修正前的實查（可重跑）：**

```
python3 scripts/harness/sensor_self_certification.py --root scripts/harness/selftest/selfcert_clean
python3 scripts/harness/sensor_governance_text.py   --root scripts/harness/selftest/gov_clean
```

→ 前者吐 **2 筆** WARN、後者吐 **3 筆**，**而 `run_selftest.py` 對兩者都印 ✅。**

**修了什麼：**

1. `expect()` 新增 **`forbid`**：列出該乾淨樣本**絕不得出現**的 finding code。
   五個「不誤報」樣本全部補上，代碼取自各自配對的「必須抓到」測試。
2. ⚠️ 判準刻意**不是**「不得有任何 WARN」——fixture 目錄本來就不含全部 glob，
   `SCAN_GLOB_MATCHES_NOTHING` 是基礎建設噪音。
   **把噪音當誤報，會逼下一個人加豁免，而豁免會把整套自測挖成盲區。**
3. ⛔ **但容忍必須看得見**：結尾列印被容忍的 WARN 代碼與筆數。
   **靜默容忍與沒有容忍，在畫面上長得一樣。**

⚠️ **首版把「該抓到的」也算進容忍，數字因而失去意義（18 → 實為 13）。**
已扣除 `needle` 指名的代碼；**該錯誤保留在程式碼註解中，不刪。**

**成對樣本在哪：** 沿用既有 12 個 fixture。⚠️ **本次未新增 fixture**——
`forbid` 是對既有樣本加上第二個斷言，不是新的檢查對象。
⛔ 若日後有人放寬 `forbid`，須說明放寬的是哪一個代碼、為什麼。

---

## #3 ｜ 2026-08-19 ｜ 猜想感測器：兩版對齊，並修掉對齊過程挖出的五個缺陷

**觸發個案：** 兩版**不對等**。`sensor_conjecture_ledger.py` 中文版 13,839 bytes、
英文版 7,065 bytes，**英文版少了三整項檢查**：

| 缺少的檢查 | 它管什麼 |
|---|---|
| `CITATION_NOT_IN_LEDGER` | 其他文件引用了台帳中不存在的編號——**失效家族「猜想漂變為前提」的主要機械防線** |
| `CONJECTURE_STATUS_INVALID` | 狀態不在台帳 §0.1 的六種之內 |
| `EVIDENCE_MISSING_FOR_STATUS` | 🟢／🔴 但依據為空 |

⚠️ 而英文版台帳 §0.4 逐字寫著「a sensor will **silently** treat it as a citation of
conjecture C-01」——**它描述了一個自己那一版沒有的行為。**

### 🔴 移植過程挖出的五個缺陷（**全部在中文版原始碼裡**）

| # | 缺陷 | 形狀 |
|---|---|---|
| 1 | `VALID_STATUS` 只有 🔵🟡🟢🔴 **四種**，而台帳 §0.1 定義**六種** | **一筆正確除役（⚫）或休眠（🟤）的猜想會被判 FAIL**——`R-19` 明文禁止的假陽性 |
| 2 | 反證條件檢查**沒有為 ⚫🟤 設豁免** | ⚠️ **與 1 互相遮蔽**：舊碼在狀態不合法時 `continue`，所以 ⚫🟤 從來走不到這裡。**只修一個會讓另一個當場現形** |
| 3 | 檔內硬編一份 `EXCLUDED_DIRS`，把 `import excluded` 蓋掉 | 與 `framework_config` 重複且**已不同步**（少 `node_modules`、`.venv`），且含一個**另一個專案的資料夾名**。違反憲章 §3.2 與 `R-21` |
| 4 | 台帳自我排除寫 `p.name != LEDGER_NAME`，而 `LEDGER_NAME` 是**路徑** | 永遠不等於裸檔名 → **台帳一直在掃描範圍內，`SCAN_GLOB_MATCHES_NOTHING` 永遠不可能觸發**。一個觸發不了的閘門與死豁免同型 |
| 5 | 掃描落空判 `FAIL` | 「掃不到」是**查不了**，應為 INCOMPLETE（`R-22`、憲章 §7.4） |

### 裁決 9 一併落地：刻意留白 ≠ 忘記填

台帳 §0.3 第 1 條逐字寫著「填『—』不會比亂填更糟，這是刻意設計的」，
而拆解模板第 3 條教人「反證條件填『—』，另在下一行寫 `**填不出來的理由：**`」——
**舊訊息對這種填法印的是「反證條件為空」，與忘記填一字不差。**

> ⚠️ **後果很難察覺：下一輪的人看到「為空」，很可能就去把它填上，
> 而那正是 §0.3 第 1 條想擋的行為。**

→ 新增 `FALSIFICATION_DECLARED_UNFALSIFIABLE`。
⚠️ **欄位名取自模板既有措辭**（zh `填不出來的理由`／en `Why I could not fill this`）——
⛔ 首版我在英文版自創了 `Why it cannot be stated`，**與模板不同步；已改正。
那正是憲章 §3.2 新增的「欄位名同樣是規則」在管的事。**

### 成對樣本（**六組，兩版各一份，共 12 個 fixture 目錄**）

| fixture | 必須抓到 | 必須不誤報 |
|---|---|---|
| `conj_status_bad` | `CONJECTURE_STATUS_INVALID` | — |
| `conj_retired_dormant` | — | ⛔ 不得出現 `CONJECTURE_STATUS_INVALID`／`FALSIFICATION_UNADJUDICATED`／`EVIDENCE_MISSING_FOR_STATUS` |
| `conj_evidence_missing` | `EVIDENCE_MISSING_FOR_STATUS` | — |
| `conj_citation_ghost` | `CITATION_NOT_IN_LEDGER` | — |
| `conj_citation_ok` | — | ⛔ 不得出現 `CITATION_NOT_IN_LEDGER` |
| `conj_declared_unfalsifiable` | `FALSIFICATION_DECLARED_UNFALSIFIABLE` | ⛔ 不得出現 `FALSIFICATION_UNADJUDICATED` |

⚠️ **`conj_retired_dormant` 是回歸測試，不是新功能測試**——它守的是缺陷 1＋2 那一對。

⚠️ **既有的四個 `conj_*` fixture 各補了一份 `NEXT_SESSION_MEMO.md`。**
理由：缺陷 4 修好之後，只含台帳一個檔的 fixture 會使引用掃描落空 → INCOMPLETE。
**一個只有一個檔案的 fixture，不代表一個真實專案。**

⚠️ **英文版四個既有 fixture 的狀態欄原本沒有表情符號**（寫 `| Conjecture |`），
補上時第一次改到了 §0.1 表格列而不是標題行——**是 fixture 實跑當場抓到的，不是我看出來的。**

**自測：兩版皆 18/18 通過，退出碼 0。**

---

## #4 ｜ 2026-08-19 ｜ 自我背書感測器的少報，與 `deny` 鍵（裁決 2）

### 4a. 🔴 自我背書感測器對一份真實審計報告判 PASS

**觸發個案（`[實測]`，標本來自另一個實際運行的專案）：**
一份跨模型 Git 系統審計報告，結論寫著：

| 該報告的用詞 | `governance/Audit_Protocol.md` §3 |
|---|---|
| 「具備**極高**的強健性」 | ⛔ 禁用「極高」 |
| 「已經**堵死**了多數邊緣失效」 | ⛔ 禁用「堵死」 |
| 「**無待修復**的重大漏洞」 | ⛔ 禁用「無待修復」；且 §2.1 逐字以此為 ❌ 範例 |
| 「運作**完美**」×2、「**非常強健**」 | ⛔ 禁用 |

**而本感測器判 PASS。**

**根因不是 APPRAISAL 沒抓到**（極高／堵死／無待修復三個都在裡面），
**是 `SELF` 不含審計報告實際使用的自我指涉詞**——「本輪」「本次」「本專案」（無「已」）。
共現條件因此不成立。

> ⛔ **少報比誤報危險：一個誤報會被追查，一個少報不會。**

### 4b. ⚠️ 但放寬 `SELF` 會誤報「誠實揭露」——所以只放寬 WARN 分支

實測：「本輪未完成的項目**全部**列在 §3，其中兩項尚未**查證**」
會同時命中 `SELF(本輪)` ＋ `SCOPE(全部)` ＋ `OBJECT(查證)`，
**而那是一句誠實的揭露，不是背書。**
⛔ 誤報一條誠實揭露正是 `R-19` 要擋的——**它會教人不要寫「我沒做完什麼」。**

→ **拆成兩個範圍，⛔ 不得合併：**

| 常數 | 用於 | 語意 |
|---|---|---|
| `SELF`（窄，未動） | **FAIL** 分支 | 「這份文件宣稱自己已查證無誤」 |
| `SELF_WIDE`（新） | **WARN** 分支 | 「這份文件在稱讚它所談的東西」 |

另修：`APPRAISAL` 原寫 `非常(?:穩健|完善|嚴謹)`，**獨漏 §3 明文列出的「非常強健」**；
英文版 `APPRAISAL` **完全沒有 `perfect`**，而 §3 明文禁用它。
⚠️ **本清單的單一定義處是 `Audit_Protocol.md` §3——本處只得涵蓋它並可再多，⛔ 不得少於它。**

### 4c. 裁決 2：`deny` 鍵

`framework_config.py` 新增 `"deny": ["ledgers"]`。
**憲章 §6 通則 2（⛔ 台帳與 T0 不在任何 AI 的產出區內）第一次有機械對應物。**

- ⚠️ **T0 兩份刻意不寫在 `deny` 裡**——定義處是 `t0_docs`，感測器自動併入。
  ⛔ 重寫一次就是兩個定義處（憲章 §3.2）。
- ⚠️ **只在有設 `write_scopes` 時生效。** 感測器讀 `git status`，分不出人與 AI；
  單 agent 專案硬跑會對主持人自己編輯台帳報 FAIL。
- ⚠️ **`_human` 豁免的筆數一律印出。** 靜默豁免與沒有豁免在畫面上長得一樣（「靜默過濾」家族）。

### 4d. 附帶修正：`load()` 讀錯專案的設定

`framework_config.load()` 原本一律讀 `ROOT/governance_config.json`，
而 `ROOT` 是**感測器檔案自己的位置**推得的。
→ 用 `--root` 指向另一個目錄時，**掃描範圍換了，設定卻沒換**，⛔ 且無任何訊息。
**副作用：自測 fixture 無法測任何與設定有關的行為。**
已改為 `load(root)`，由 `_common.cli()` 傳入。

### 成對樣本

| 樣本 | 必須抓到 | 必須不誤報 |
|---|---|---|
| `selfcert_appraisal`（新） | `UNSUPPORTED_GLOBAL_APPRAISAL` | ⛔ 無 `UNVERIFIABLE_SELF_CERT` |
| `selfcert_clean`（補一段誠實揭露） | — | ⛔ 兩個代碼皆不得出現 |
| **scope（四項，暫存區真 git repo）** | `WRITE_TO_DENIED_PATH` ×2 | ⛔ `_human` 涵蓋時不得誤報；範圍內變更不得誤報 |

⚠️ **`sensor_scope_and_t0.py` 在此之前沒有任何自測。**
它讀 `git status`，而在 repo 內再建 `.git` 會使它拒絕回報——**測試環境必須在本 repo 之外**，
故改在系統暫存區現建 repo，用完刪除。依 `R-27`，本節走「`write_scopes` 已設定」那一條分支。

⚠️ **首版四項測試錯了三項**：基礎檔案沒有先 commit，於是 `governance/AGENTS.md` 與
`governance_config.json` 自己也算成本輪變更。**是自測當場抓到的，不是我看出來的。**

**兩版自測 23/23 通過。**

---

## #5 ｜ 2026-08-19 ｜ 裁決 5：Prompt 自足性感測器拆成兩層

### 5a. 舊版把 DR 專用條款表套用到每一份 prompt

**實測（修正前）：框架自己隨附的三份模板全部 FAIL。**

`prompts/TEMPLATE_decompose.txt` 是一份**完全自足**的拆解 prompt，
開頭逐字寫著「⛔ 這一輪不要查文獻」——
**一份禁止查文獻的 prompt，不可能也不應該包含雙語檢索條款。
它組裝完成之後仍然會 FAIL，永遠。**

> 🔴 **而 `prompts/README.md` 已經替那個誤報寫好了解釋：**
> 「因為範本裡寫的是 `<<<貼上區塊 B>>>`」——
> **那個理由對 `TEMPLATE_prior_art.txt` 成立，對另外兩份不成立**（它們沒有區塊佔位符）。
>
> **一支對正確文本報警的感測器，加上一份說「這個 FAIL 是正確的」的官方說明。**
> **第二層比第一層危險：它把「忽略這支感測器」寫成了制度。**

**→ 拆成兩層：**

| 層 | 檢查 | 適用 |
|---|---|---|
| ① 自足性 | 跨 prompt 指涉、未組裝區塊、prompt 汙染 | **所有 prompt** |
| ② DR 條款表（8 項） | LANG／VENUE-TYPE／NO-DOI／PASS A／B／標題精確性／NONE RETRIEVED／OUTPUT CONTRACT | **只有 `--profile deep-research`** |

⛔ `generic` 下一律印出「**未執行** DR 專用條款表——不適用（⚠️ 不適用 ≠ 通過）」。

### 5b. 三種 `<<<…>>>` 佔位符，只有一種是缺陷

| 形態 | 例 | 舊版 | 現在 |
|---|---|---|---|
| 區塊佔位符 | `<<<貼上區塊 B>>>` | FAIL | ❌ **FAIL**（`PROMPT_NOT_ASSEMBLED`） |
| **內容槽** | `<<<貼上你的構想全文>>>` | **FAIL（誤報）** | ✅ **不是缺陷** |
| 填空槽 | `<<<填空:…>>>` | FAIL | ⚠️ WARN |

### 5c. 🔴 引述禁令 ≠ 違反禁令

**實測：** `TEMPLATE_decompose.txt` 有一行是
「⛔ 不要在反證條件欄裡寫『待填』『見下方說明』之類的字」，
**而感測器把其中的「見下方」判成跨檔指涉。**

> **字串比對能辨認「有沒有這個詞」，不能辨認「這個詞是誰在說」。**
> **先行專案在三支不同的感測器上各撞過一次同一件事。**

→ 同一行內禁令標記（⛔／不得／不要／禁止）出現在該詞**之前**即判為引述。
⛔ **被豁免的行號一律印出**——靜默豁免與沒有豁免長得一樣（「靜默過濾」家族）。

### 5d. 兩版此前是兩支不同的感測器

中文版：CROSSREF ＋ POLLUTION ＋ 8 項 DR 條款表（全部 FAIL 級）。
英文版：CROSSREF（含佔位符）＋ 三項通用提示（WARN 級），**完全沒有 DR 條款表**。
→ 已收斂為同一設計，並取兩者之長。

### 5e. 通用提示以框架自己的模板校準

⛔ 若某項對一份寫得好的 prompt 報警，那是樣式錯了（`R-19`）。
⚠️ 中文版的列舉樣式原本含裸的「列出」，**會命中散文**（對抗模板的「沒有被列出來」），
使一份其實沒有列舉要求的 prompt 看起來合格——已移除。
⚠️ 英文版原本沒有 `answer each`，**而對抗模板正是這樣寫的**——
一項提示在一版命中、另一版不命中，**那是漂移，不是語言差異**。已補。

### 5f. 附帶修正：禁用清單漂移

`TEMPLATE_adversarial.txt`（中文版）寫「非常**穩健**」，
而 `governance/Audit_Protocol.md` §3 寫「非常**強健**」。**已對齊。**

⚠️ **這裡有一個真實的張力，值得記下來：**
`R-24` 要求每份 prompt 完全自足（所以模板**必須**把禁用清單抄一份），
而憲章 §3.2 要求每條規則只有一個定義處。**兩者在此互斥，而互斥的結果就是漂移。**
⛔ **目前沒有機械防線。** 這是一個待裁決事項。

### 成對樣本（**七項，涵蓋兩個 profile —— `R-27`**）

| 樣本 | generic | deep-research |
|---|---|---|
| `prompt_crossref` | ❌ `CROSS_PROMPT_REFERENCE` | — |
| `prompt_unassembled` | ❌ `PROMPT_NOT_ASSEMBLED` | — |
| `prompt_clean` | ✅ PASS，⛔ 無 DR 代碼 | ❌ `DR_CLAUSE_MISSING`（**分支測試**） |
| `prompt_quoted_prohibit` | ✅ PASS ＋ 須印出豁免（**回歸**） | — |
| `prompt_content_slot` | ✅ PASS（**回歸**） | — |

**兩版自測 30/30 通過。**

### ⚠️ 一個真陽性，留給主持人裁決

修正後，`TEMPLATE_adversarial.txt` 兩版**各剩一個 WARN：找不到幻覺護欄。**
**那不是誤報**——對抗審計模板確實沒有任何「不確定就明說、不要編」的條款。
⛔ 是否補上是內容決定，**本輪不自行加入。**

---

## #6 ｜ 2026-08-19 ｜ 裁決 8（遲了五輪）：提案檔的引用豁免；裁決 19：對抗模板補幻覺護欄

### 6a. 裁決 8：`CITATION_NOT_IN_LEDGER` 對提案檔豁免

**觸發個案：框架自己的流程與自己的感測器互斥。**

流程是「AI 提案 → 人裁決 → 人寫入台帳」（`governance/AGENTS.md` §5），
於是在「提案已交、裁決未下」的那段期間，**提案檔必然引用尚未存在的編號**，
而 `CITATION_NOT_IN_LEDGER` 必然報 FAIL。**實測一輪 12 筆。**

→ 這正是 `profiles/PROFILE_multi_agent.md` §4.3 **G-1b** 問的那個問題：
**「新規則會不會與某條既有規則在『同時遵守』時互相排斥？」**
⚠️ **而這次互斥的兩方，一方是流程規定，一方是感測器。**

**判準（⛔ 結構性，不是自由文字）：** 位於 `handoffs/` ＋ 檔名含 `proposal_markers` 之一。
標記的單一定義處是 `framework_config.py`。

⛔ **豁免只關掉本項檢查，其餘照跑。⛔ 被豁免的檔案一律印出**（`CITATION_CHECK_EXEMPTED`）。

⚠️ **為什麼判準不能是「寫一句理由」：** 先行專案的豁免欄只要求一句理由，
**於是一句讀起來合理的話讓感測器對一筆真捏造永久靜默，而儀表板是綠的。**
**自由文字的理由無法被機械檢查。**

### 6b. ⚠️ 這一項是「已裁決但沒做」，欠了五輪

**主持人在第三工作段就裁決 [A]，而我到第九工作段才做。**
沒有任何地方顯示它還欠著——**因為我的治理文件沒有待裁決佇列**，
而憲章 §5.1 明文要求它，且我在第一輪就讀過那一節。

→ 已建 `本地工作區/待裁決.md`，含「已等待」欄，並把
**「已裁決」與「已落地」分成兩張表**——把它們放同一張表，正是這一項掉掉的原因。

### 6c. 裁決 19：`TEMPLATE_adversarial.txt` 補幻覺護欄

修正 #5 之後，兩版對抗模板各剩一個 WARN：**找不到幻覺護欄**。
**那不是誤報**——該模板確實沒有任何「不確定就明說、不要編」的條款。
主持人裁決補上。現在兩版皆 **0 FAIL 0 WARN**。

補的內容重點：**審計者捏造一個發現，比漏掉一個發現更糟**——
它會讓人去修一個不存在的問題，而且會消耗掉下一次審計的信任。

### 成對樣本

| 樣本 | 必須抓到 | 必須不誤報 |
|---|---|---|
| `conj_citation_ghost`（既有） | `CITATION_NOT_IN_LEDGER` | **守「豁免不得過寬」** |
| `conj_citation_proposal`（新） | `CITATION_CHECK_EXEMPTED` | ⛔ 無 `CITATION_NOT_IN_LEDGER` |

**兩版自測 31/31 通過。**

---

## #7 ｜ 2026-08-19 ｜ 裁決 12（重寫模型歸屬）與裁決 14（新增引用完整性）

### 7a. 裁決 12：`sensor_model_attribution.py` 整支重寫

**舊版的問題不是白名單，是掃描範圍。**

舊版掃「專案根目錄所有 `.md`」。**而根目錄的 `.md` 是框架提供的模板
（`README`／`SETUP`／`file_index` …），不是 AI 的產出物——它們本來就不該有作者欄。**
於是它必須靠兩份硬編白名單（13 筆＋12 筆，全是另一個專案的檔名）壓住自己製造的告警，
並且**在全新乾淨模板上第一次執行就報三筆 WARN**。

> **那張白名單是為了補償一個錯的掃描範圍而存在的。**
> **範圍改對，白名單就不需要了——這一版一筆硬編檔名都沒有。**

| | 舊版 | 新版 |
|---|---|---|
| 掃描範圍 | 根目錄 `.md` ＋ `handoffs/`（硬編） | `attribution_globs`（發現式，預設 `handoffs/*.md` 等） |
| 硬編檔名 | **25 筆** | **0** |
| 懸空引用 | **5 筆章節 ＋ 4 筆檔案** | **0** |
| 「具體型號」判準 | 一份型號清單（會過期） | **結構性：是否帶版本數字** |
| 乾淨模板上的輸出 | 3 筆假 WARN | 3 筆**死 glob** WARN（真實陳述） |

⚠️ **「具體型號」刻意不列舉型號清單**——那是另一種白名單。
判準改為：**具體型號幾乎必然帶版本數字**（`opus-5`、`gemini-3.7-flash`、`o4-mini`），
**家族名與平台名不帶**（`Claude`、`Gemini`、`Antigravity`）。
平台名另以一份**取值域**清單輔助（⛔ 取值域不是掃描範圍，故不違反 `R-21`）。

🔴 **`MODEL_UNREADABLE_DECLARED` 是本次最重要的一項：**
`policy/MODEL_IDENTITY.md` §3.6 規則 1 逐字說，標記不在可見上下文內時，
**唯一正確的輸出**是 `[Model: 無法讀取——…]`。
⛔ **判它 FAIL 會逼下一個模型退回寫平台名，而 §3.6 規則 2 說那比留空更糟。**
**降級的方向必須是「不知道」，不是「比較模糊的名字」。** → 判 WARN 並提醒補填。

### 7b. 裁決 14：新增 `sensor_reference_integrity.py`

**既有的 `sensor_governance_text.py` 只查章節能不能解析，而且只掃 `.md`。**
兩個缺口一直開著：`.py` 檔頭從未被掃過；**「被引用的檔案存不存在」從未被查過。**

**上線前的盤點：兩版合計 11 種懸空檔案引用**，其中：

| 來源 | 筆數 | 性質 |
|---|---|---|
| 舊 `sensor_model_attribution.py` | 2 | 另一個專案的路徑 |
| `policy/MODEL_IDENTITY.md` | 3 | 他專案路徑 ×2 ＋ **一個框架承諾卻未隨附的檔案** |
| 格式說明的佔位符 | 4 | ⚠️ **不是缺陷** |
| **我在重寫 7a 時新造的** | 2 | ⚠️ **見下** |

🔴 **最後那兩筆值得單獨記：我在重寫感測器的檔頭時，把舊白名單的檔名當例子寫進註解，
於是那些檔名立刻成為新的懸空引用。**
**「描述缺陷時不要實例化它」——先行專案在同一機制上撞過三次，這是第四次。**
⛔ **處置與先行專案相同：改寫為敘述，不加豁免。**

**兩種不是缺陷的引用，處理方式不同：**

| 形態 | 判準 | 處理 |
|---|---|---|
| 格式佔位符 `` `<檔名>.md` `` | **尖括號** | ⛔ 不檢查 |
| 明示未隨附 | **同一行有顯式標記** | ✅ 豁免，**但必須印出來** |

⛔ **豁免的判準是「同一行有標記」，不是一份豁免清單**——
先行專案前兩次靠改寫措辭迴避，第三次那個字串是實質內容無法迴避，
**於是改用顯式標記並要求標記本身被列印。** 本感測器沿用。

**上線後：兩版對真實模板皆為 PASS**（zh 有 1 筆已印出的顯式豁免，en 為 0）。

### 成對樣本

| 樣本 | 必須抓到 | 必須不誤報 |
|---|---|---|
| `attrib_missing` / `attrib_vague` / `attrib_platform` | 對應代碼 | — |
| `attrib_clean` | — | ⛔ 兩個代碼皆不得出現 |
| **`attrib_unreadable`** | `MODEL_UNREADABLE_DECLARED` | ⛔ **不得判為缺陷** |
| `ref_dangling` | `DANGLING_FILE_REF` | — |
| `ref_ok` / `ref_placeholder` | — | ⛔ 無 `DANGLING_FILE_REF` |
| `ref_not_shipped` | `REF_EXEMPTED_NOT_SHIPPED` | ⛔ 無 `DANGLING_FILE_REF` |

**兩版自測 40/40 通過；`run_all_sensors.py` 現為 6 支，兩版皆 PASS。**

⚠️ **`sensor_reference_integrity.py` 在掛載式檔案系統上約需 5 秒**（它要走一次全專案）。
本機應遠快於此，**但若你日後覺得整套變慢，這一支是第一個要看的。**

---

## #8 ｜ 2026-08-19 ｜ 裁決 21：猜想感測器改用共用的 `cli`／`emit`

**觸發個案：** `sensor_conjecture_ledger.py`（中文版）**自己帶了一份 `argparse` 與一份 `emit()`**，
與 `_common.py` 的那份平行存在。六支感測器裡只有它是這樣。

**後果不是輸出錯，是規則有兩個定義處：**

| 東西 | 定義處 A | 定義處 B |
|---|---|---|
| `--root`／`--json` 介面 | `_common.cli()` | 本檔自己的 `argparse` |
| 退出碼語意（0／1／2） | `_common.emit()`（憲章 §7.4） | 本檔自己的 `emit()` |
| JSON 輸出格式 | `_common.emit()` | 本檔自己的 `json.dumps` |

⚠️ **憲章 §3.2 管的就是這件事。** 兩份實作今天一致，**不代表下一個人改其中一份時也會一致**——
而改錯的那一次不會有任何訊息。

### ⛔ 我對這一項的預測是錯的，記在這裡

提請裁決時我寫的理由是「本檔的 `emit()` 收一個**獨立的 `incomplete` 布林參數**，
而獨立布林與 findings 可能不一致」。
**實查後不成立：舊碼寫的是 `incomplete = incomplete or any(f[0] == "INCOMPLETE" for f in findings)`，
它一樣從 findings 導出。**

> ⚠️ **一個看起來很像既有失效家族的形狀，不等於它就是那個家族。**
> 我是在動手前才去讀那一行的；**如果順序反過來，這份記錄會寫著一個不存在的缺陷，
> 而且讀起來完全合理。**

→ 已在裁決落地前向主持人更正。**本項的真正理由只有一個：重複的定義處。**

**修了什麼：**

- 刪除本檔的 `emit()`、`argparse` 區塊與 `import argparse`／`import json`（改用後即為死 import）。
- 改為 `root, _cfg, as_json, _name = cli("conjecture_ledger")` ＋
  `emit("猜想台帳感測器", findings, stats, as_json, "conjecture_ledger")`。
- ⛔ **不再傳獨立的 `incomplete` 參數**——INCOMPLETE 一律由 findings 導出，只此一途。

**成對樣本在哪：** ⚠️ **本次未新增 fixture，這是刻意的。**
本項是**行為不變的重構**，既有六組 `conj_*` 成對樣本就是它的回歸測試——
**若行為變了，那六組會當場變色。**
⛔ 若日後有人改動 `_common.emit()` 的退出碼語意，須回頭確認這六組仍然成立。

**依裁決 21「單獨做，同一批不夾帶任何其他改動」執行。兩版自測 43/43 通過。**

---

## #9 ｜ 2026-08-19 ｜ 裁決 20：掃描落空的三態，與退出碼聚合

### 9a. 🔴 退出碼聚合會把 INCOMPLETE 吃掉

`run_all_sensors.py` 的聚合原本讓**後跑的結果覆蓋前面的**。
**實測（可重跑）：一組 `[2, 1]` 的退出碼聚合成 `1`。**

> **於是「有一支感測器根本沒查成」被一支 FAIL 蓋掉，而修完那個 FAIL 之後整套變綠——
> 那支沒查成的感測器，從頭到尾沒有人知道它沒查成。**

→ 改為 `worst = max(worst, r.returncode)`。⚠️ 語意上這依賴 `0 < 1 < 2` 的排序**恰好**
與嚴重度一致；已在該行註記，**若日後新增退出碼，這個巧合會失效**。

### 9b. 裁決 20：掃描落空不再一律只是 WARN

舊行為：任何 glob 命中 0 個檔案 → `SCAN_GLOB_MATCHES_NOTHING`（WARN，**退出碼 0**）。

⚠️ **問題是「命中 0 個」有兩種完全不同的意思：**

| 情形 | 意思 | 該不該讓整套變色 |
|---|---|---|
| 目錄還不存在 | 這個專案還沒用到這一塊 | ⛔ 不該——會讓新專案第一次執行就滿江紅 |
| 目錄剛建好、還空著 | 同上 | ⛔ 不該 |
| **目錄裡有同副檔名的檔，而 glob 一個都沒掃到** | **樣式與現實脫節了——這一層防線正在空轉** | ✅ **該（INCOMPLETE）** |

> **「查無比對對象」與「比對後一致」在輸出上長得一樣**（`R-22`、憲章 §7.4）。

**判準寫了三次才對，三次都留在這裡：**

| 版本 | 判準 | 為什麼錯 |
|---|---|---|
| 一 | 目錄存在 → 崩塌 | 框架自己空的 `handoffs/` 當場 INCOMPLETE |
| 二 | 目錄裡有任何檔 → 崩塌 | 純 `.py` 專案的 `scripts/**/*.sh` 被判崩塌 |
| **三（採用）** | **目錄下存在同副檔名的檔，而 glob 一個都沒掃到** | — |

⚠️ **另修 `_glob_dir()`：** 首版對**不含萬用字元**的 glob（如 `file_index.md`）回傳專案根目錄，
於是「任何缺少該檔的 fixture」全部變成崩塌——**自測當場 7 項回歸**。
→ 無萬用字元時回傳 `None`（那種 glob 命中 0 個就只是檔案不存在，不是覆蓋率崩塌）。

**成對樣本（三項，兩版各一份）：**

| 樣本 | 期望 |
|---|---|
| `目錄不存在` | ⚠️ WARN，退出碼 0 |
| `目錄剛建好還空著` | ⚠️ WARN，退出碼 0 |
| `目錄有東西但沒一個符合 glob` | ❌ **INCOMPLETE（退出碼 2）** |

⚠️ **中間那一項是「必須不誤報」的那一半**——⛔ 沒有它，判準會被下一個人放寬回版本一。

---

## #10 ｜ 2026-08-19 ｜ 裁決 17：重複長句——真正的缺陷不在那兩句話，在斷句器與門檻

### 10a. ⚠️ 裁決前提是錯的，先講這個

提請裁決 17 時我寫的是「英文版有兩處跨檔重複長句，中文版沒有」。
**實查後不成立：兩版重複的是同一批文字，只是中文版的感測器看不見。**

| | 中文版 | 英文版 |
|---|---|---|
| `MIN_DUP`／`MAX_DUP` | **24 / 80** | **40 / 220** |
| 那句話正規化後長度 | **21 字元** | **68 字元** |

**中文那句差三個字就落在自己那一版的門檻外**，於是「英文版比較髒」是量出來的假象。
⛔ **兩版門檻不同這件事，沒有登記在任何地方。**

> 🔴 **一支較弱的感測器，會被讀成一份較乾淨的文件。**

### 10b. 字元數門檻對中文系統性偏弱 → 改用資訊長度

同樣的內容，中文大約用三分之一的字元寫完。
→ `info_len()`：**一個 CJK 字元計為三個拉丁字元**；兩版統一為 `MIN_DUP, MAX_DUP = 40, 220`。

### 10c. 斷句器只認 `。` 與換行 → 英文版只能比對「整行相同」

英文句子以 `.` 結尾，而 `.` 不是斷點，於是**英文版實際上只在做整行比對**。
`Audit_Protocol.md` 與 `HANDOFF.md` 共用的那一句，中文版抓到、英文版抓不到。

→ 斷點補上「句號後接空白」。
⚠️ **首版仍然抓不到**：`**…unfinished.** "None"…` 的句號後面是 `*` 不是空白。
→ 判準改為「句號 ＋ 可選的收尾符號（`**`、引號、括號）＋ 空白」。
`3.2`、`.md` 不會被切開（且行內程式碼已先移除）。

**修正後英文版當場抓到那一句——它一直都在，只是量不到。**

### 10d. 檔頭標語不是規則（⛔ 這不是為了誤報而放寬判準）

`Claim_Ledger.md` 與 `Conjecture_Ledger.md` 的檔頭同樣寫著「T1 資料類。AI 不得直接寫入本檔。」，
於是被判為「同一規則有兩個家」。
**那一行是檔案的中繼資料，不是規則**——每個資料類檔案都必須各自寫上它。
**要求它只出現一次，等於要求它不成立。**

⛔ **`R-20` 禁止的是「因為報警所以放寬判準」；這裡改的是判準的對象**，理由寫在原始碼註解裡。
判準仍是結構，不是關鍵字：**整行只有一個粗體段，且位在第一個 `## ` 之前。**

### 10e. 裁決 17 的落地：`Claim_Ledger.md` §1.1 第 4 條改為引用

⚠️ **（§1.1 已於 v1.4.1 搬至 `governance/CLAIM_LEDGER_SPEC.md`。⛔ 標題保留原文。）**

R-27／R-28 的內容原本在 `governance/RULES.md` 與 `ledgers/Claim_Ledger.md` **各寫了一遍**
（含 `individ-\nual` 那個例子）。⚠️ **兩版皆然。**
→ 台帳改為指向 `governance/RULES.md` R-27、R-28，**只保留台帳特有的那一句後果**
（改寫提取物會使雜湊防線失效）。

⛔ **不採「把其中一句改寫掉」**——先行專案 I-42／I-43 明文拒絕以改寫措辭迴避重複。

### 10f. 順帶修掉一個錯的規則引用

`run_selftest.py` 兩版各有兩處寫著「依 `R-27`：有分支的感測器，每條分支都要有自測走過」。
**`R-27` 是正規化順序，不是這件事**；該要求的定義處是憲章 §7.1。
→ 四處全部改為 `governance/WORKFLOW_CONSTITUTION.md` §7.1。**這正是 `R-16` 在管的形狀。**

### 成對樣本

| 樣本 | 必須抓到 | 必須不誤報 |
|---|---|---|
| `gov_dup`（既有） | `DUPLICATE_RULE_TEXT`（整行相同） | — |
| **`gov_dup_midline`（新）** | `DUPLICATE_RULE_TEXT`（**句子結束在行中央**） | ⛔ 刻意不含整行相同的那一種 |
| `gov_clean`（**兩檔各補一行相同的檔頭標語**） | — | ⛔ 不得出現 `DUPLICATE_RULE_TEXT` |

⚠️ **`gov_clean` 那一行是這樣驗證有效的：** 暫時把 `strip_banner()` 換成恆等函式重跑，
**兩版都當場冒出 `DUPLICATE_RULE_TEXT`**；換回來就沒有。
⛔ **一個「加了豁免之後永遠會過」的樣本，測不到任何東西。**

**兩版自測 44/44 通過。**
**現況：`sensor_governance_text.py` 中文版 2 筆 WARN、英文版 1 筆——⚠️ 全部是真陽性，見下。**

### ⚠️ 兩項真陽性，留給主持人裁決（**未自行處理**）

| # | 重複之處 | 為什麼我不自己決定 |
|---|---|---|
| a | `Audit_Protocol.md`／`HANDOFF.md`：「寫『無』不是覆蓋率完整，是沒有盤點」（兩版皆有） | **兩邊都需要它，而且各自的前一句不同**（一句講審計覆蓋率，一句講交接未完項）。定義處要選哪一邊是內容決定 |
| b | `Incident_Log.md`／`RULES.md`：自我背書那一則個案（**只有中文版被抓到**） | 英文版同樣重複，但措辭差一個字（`among` 對 `in`），**字面比對抓不到**——⚠️ 這正是本感測器自己聲明過的盲區 |

---

## #11 ｜ 2026-08-19 ｜ 裁決 18：新增 `sensor_clause_sync.py`（條款清單同步）

**觸發個案（登錄於 #5f，當時無機械防線）：**
`prompts/TEMPLATE_adversarial.txt` 寫「非常**穩健**」，
而 `governance/Audit_Protocol.md` §3 寫「非常**強健**」。

**一個字的差別，使那份 prompt 的禁用清單少防一個詞，而兩份文件都讀起來完全正常。**
⚠️ **是人逐字比對才發現的，而且是在做別的事情時順手看到的。**

### 為什麼這個漂移是**結構性**的，不是誰不小心

| 規則 | 要求 |
|---|---|
| `R-24` | 每份 prompt **完全自足**——⛔ 不得寫「同上」「參見前述」 |
| 憲章 §3.2 | **每條規則只有一個定義處** |

> 🔴 **兩者在此互斥：自足迫使你抄一份，單一定義處禁止你抄一份。**
> **互斥的結果不是有人違規，是漂移——而漂移不會有任何紅燈。**

⛔ **本感測器不解決那個互斥。** 哪一份是定義處、要不要抄，仍然是人的決定。
它只回答一個機械問題：**「抄過去的那一份，今天還和定義處一樣嗎？」**

### ⚠️ 我對裁決 18 字面的偏離：判準是「集合相等」，不是「逐字逐序相同」

裁決 18 的字面是「比對……是否**逐字一致**」。
**實查：兩版的禁用清單內容相同而順序不同**（英文版定義處 `airtight` 排第四，模板排最後）。

> **順序在這份清單裡不承載任何意義。對一個無意義的差異報 FAIL，
> 正是 `R-19` 說的那種會教人關掉感測器的誤報——而且它會在上線第一天就發生。**

→ **放寬的是順序，⛔ 不是字。** 每個項目仍逐字比對，
「非常穩健」與「非常強健」是兩個不同的項目，集合相等當場不成立。
⚠️ **這一點寫進了 fixture：** `sync_ok`（順序不同）**必須不誤報**——
⛔ 沒有它，下一個人會把判準「修正」回逐字逐序，而那會對兩版現況報 FAIL。

### ⛔ 零硬編副本檔名

| | 做法 |
|---|---|
| 定義處 | `synced_lists[].home`——**一個指標**，⛔ 不是掃描範圍，故不違反 `R-21` |
| 副本 | 由 `marker` **發現**：掃描範圍內任何帶標記的行都是副本 |
| 掃描範圍 | `sync_scan_globs`（⛔ **不含 `scripts/`**——變更記錄與原始碼**描述**這份清單，而描述不是副本） |

**加一份新模板不需要改這支感測器；漏改的那一份會自己現形。**

### 成對樣本

| 樣本 | 必須抓到 | 必須不誤報 |
|---|---|---|
| `sync_drift` | `SYNC_LIST_DRIFT`（一字之差） | — |
| **`sync_ok`** | — | ⛔ **順序不同不得誤報**（`SYNC_LIST_DRIFT`／`SYNC_HOME_MISSING`／`SYNC_NO_COPY` 皆不得出現） |
| `sync_home_missing` | `SYNC_HOME_MISSING`（**INCOMPLETE，⛔ 不是 PASS**） | — |

⚠️ **`SYNC_NO_COPY` 是 WARN 而不是靜默：** 定義處有清單卻找不到任何副本，
表示這一項**目前沒有在守望任何東西**——與死 glob 同型。

**兩版自測 47/47 通過；`run_all_sensors.py` 現為 7 支，兩版皆 PASS。**

---

## #12 ｜ 2026-08-20 ｜ 指名了定義處的句子，不是「第二個定義處」

**觸發個案（本輪實測，⚠️ 由我自己造成）：**
為了消除一句在三個檔案裡重複的規則，我把其中兩處改寫成
「**見憲章 §3.4，⛔ 本處不重述**」。
**於是那句「引用」本身在兩個檔案裡逐字相同，感測器又把它判為重複。**

> 🔴 **但那是反過來的：一句指名了定義處的話，⛔ 不可能是第二個定義處——
> 它正是防止第二個定義處存在的那個機制。**

⛔ **這不是「因為誤報所以放寬判準」（`R-20`），是把判準的對象修正回規則本身**
——與 #10d 的檔頭標語同一種修正。

**判準（結構，⛔ 不是關鍵字）：** 句中出現 `§` 章節號或 `R-nn` 規則號 → 視為指標，不參與比對。

⚠️ **代價寫明：** 一條**同時指名了某個章節**的真重複規則會被漏掉。
**本感測器選擇漏掉它，⛔ 而不是對每一個引用報警**（`R-19`：會對正確文本報警的感測器會被關掉）。

### 成對樣本

| 樣本 | 必須抓到 | 必須不誤報 |
|---|---|---|
| `gov_dup`／`gov_dup_midline`（既有） | `DUPLICATE_RULE_TEXT`（**兩者都不含 `§`**） | — |
| `gov_clean`（**兩檔各補一句逐字相同的純引用**） | — | ⛔ 不得出現 `DUPLICATE_RULE_TEXT` |

⚠️ **這個樣本是這樣驗證有效的：** 把 `POINTER` 換成永不命中的樣式重跑，
**`DUPLICATE_RULE_TEXT` 當場冒出來**；換回來就沒有。
⛔ **一個加了豁免之後永遠會過的樣本，測不到任何東西。**

⚠️ **首版的 fixture 引用了 `§3.4`，而 fixture 自己的憲章只有 `§1`**——
**於是 `SECTION_REF_UNRESOLVED` 誤報，自測當場擋下。**
**是自測抓到的，不是我看出來的。**

**兩版自測 47/47 通過；`run_all_sensors.py` 兩版 exit 0。**

---

## #13 ｜ 2026-08-20 ｜ 自足性：本資料夾必須能被單獨複製出去使用

🔴 **觸發個案（本輪實測，⚠️ 由我自己造成，且我自己發現）：**
框架說明圖第一版放在**倉庫根目錄**的 `docs/`，而各版 README 以 `../docs/framework.svg` 引用它。

> **在倉庫裡看起來完全正常。**
> ⛔ **但使用者的實際用法是把 `Spark2Groundwork_<zh|en>/` 整包複製到自己的專案——
> 複製出去之後那張圖就不存在了，而 README 仍然理直氣壯地引用它。**

⚠️ **這一類依賴的形狀：它在原地永遠不會報錯，只在別人手上壞掉。**
**而「在我這裡是好的」與「在你那裡也是好的」，在我的畫面上長得一樣。**

**修了什麼：** `sensor_reference_integrity.py` 新增 `REF_ESCAPES_EDITION`（FAIL）——
任何 `](../…)`、`src="../…"`、`href="../…"` 一律判為跳出本資料夾。

⚠️ **判準是路徑，⛔ 不是意圖。**
**`scripts/harness/xxx.sh` 裡的 `cd "$(dirname "$0")/../.."` 不受影響**——
那是 shell 的工作目錄計算，不是文件引用，**兩者的形狀不同，不需要豁免清單去區分。**

### 成對樣本

| 樣本 | 必須抓到 | 必須不誤報 |
|---|---|---|
| `ref_escapes` | `REF_ESCAPES_EDITION` | — |
| `ref_selfcontained` | — | ⛔ 無 `REF_ESCAPES_EDITION`／`DANGLING_FILE_REF` |

**兩版自測 49/49 通過；`run_all_sensors.py` 兩版 exit 0。**

⚠️ **同一輪順帶補上的兩件事（⛔ 不是感測器變更，記在這裡是為了脈絡完整）：**
① 兩版 README 各自補上授權說明——**原本 `LICENSE` 只在倉庫根，複製出去就沒有了**；
② 根目錄 `.gitattributes` 補上 `*.command`——**我前幾輪只加進兩版內的那兩份，漏了根目錄那一份。**

---

## #14 ｜ 2026-08-20 ｜ 🔴 整套 harness 在繁體中文 Windows 上無法執行

**觸發個案：主持人在自己的 Windows 機器上跑第一次發布前檢查。**

```
UnicodeEncodeError: 'cp950' codec can't encode character '✅'
```

### 為什麼會這樣

**Python 在 Windows 上的預設輸出編碼是系統 ANSI 碼頁**（繁中為 `cp950`），
而本框架的**每一則訊息**都含 `✅`／`⚠️`／`⛔`。
**於是感測器印到第一個符號就當掉。**

⚠️ **本框架此前是靠 `.bat` 開頭的 `chcp 65001` 才能在 Windows 上跑**，
而 v1.2.0 把 `.bat` 改成薄殼時 **那一行沒有被帶過去**——
🔴 **並且使用者只要不透過按鈕、直接 `python scripts\harness\…` 執行，就一樣會當。**
**⛔ 靠主控台碼頁的修法從來就不是修法，只是剛好沒被踩到。**

### 🔴 更嚴重的是它當掉的方式

| 現象 | 真相 |
|---|---|
| 畫面印「**SUMMARY: FAIL（7 支已執行）**」 | **兩支感測器根本沒跑完** |
| 每一支都印了統計，但那兩支**沒有 `Result:` 那一行** | 它們死在中途 |

**Python 未捕捉例外一律 `exit 1`，而 1 的意思是「找到缺陷」。**
⛔ **於是「感測器壞了」被呈現成「文件有問題」——
那正是 `R-22` 與憲章 §7.4 明文要擋的混淆，發生在執行器自己身上。**

⚠️ **舊的崩潰偵測 `if returncode not in (0,1,2)` 在結構上不可能抓到 Python 崩潰**，
**因為 Python 崩潰的退出碼永遠是 1。**

### 修了什麼

| # | 修正 |
|---|---|
| 1 | **`_common._force_utf8()`**——把 `stdout`／`stderr` 重設為 UTF-8（`errors="replace"`，⛔ 最壞印 `?` 而不是當掉），Windows 上並設 `SetConsoleOutputCP(65001)`。**`_common` 匯入時即執行**，所有感測器自動受惠 |
| 2 | 七支執行器與工具、以及不經 `_common.cli` 的 `sensor_prompt_self_contained.py`，各自明確呼叫一次 |
| 3 | 所有 `subprocess.run(..., text=True)` **一律指定 `encoding="utf-8"` 並傳 `PYTHONIOENCODING=utf-8` 給子行程**——⚠️ 只設一半會變成 `UnicodeDecodeError` |
| 4 | **崩潰判準改為結構性：非 0 退出 ＋ stderr 含 `Traceback (most recent call last)` ＝ 崩潰 → INCOMPLETE** |
| 5 | `.bat` 薄殼補回 `chcp 65001`——⚠️ **不是為了防當掉（那已由 ①② 解決），是為了主控台不要顯示亂碼** |

### 成對樣本

| 樣本 | 必須抓到 | 必須不誤報 |
|---|---|---|
| **`_selftest_crasher.py`（新）** | 崩潰須判 INCOMPLETE，⛔ 不是 FAIL | — |
| 既有 49 項 | — | ⛔ 在 `PYTHONIOENCODING=cp950` 下仍須全過 |

⛔ **崩潰測試刻意用一支「一定會崩潰」的假感測器，⛔ 不是用真感測器**——
**真感測器有一天會被修好，而這個測試要測的是執行器的行為。**

### ⚠️ 驗證方式（可重跑）

```
PYTHONIOENCODING=cp950 python3 scripts/harness/run_all_sensors.py
PYTHONIOENCODING=cp950 python3 scripts/harness/run_selftest.py
```

**修正前：** 感測器逐一 `UnicodeEncodeError`，總結 FAIL。
**修正後：** 兩版皆 exit 0，自測 **50/50**。

🔴 **這一則是本框架最貴的一課：**
**整套系統在我的環境裡全綠了二十幾個工作段，而它在使用者的機器上一次都沒跑起來過。**
**⛔ 「在我這裡是好的」與「在你那裡也是好的」，在我的畫面上長得一樣。**

## #15 ｜ 2026-08-26 ｜ 🔴 短式章節引用沒有任何感測器在查

### 1. 觸發個案（**三筆，⛔ 全部是用手查出來的**）

| 引用 | 位置 | 形態 |
|---|---|---|
| `憲章 §5.11` | 兩版 `sensor_model_attribution.py` 檔頭 | 🔴 **一段描述缺陷的文字，實例化了它所描述的缺陷**——⚠️ 同段上兩行才剛寫「檔名此處刻意不寫出來」 |
| `憲章 §8.1` | 兩版 `Audit_Protocol.md` | 憲章 `## 8.` 底下⛔ 沒有 `### 8.1`，引用者憑印象加了小節號 |
| `憲章 §5.8` | 中文版 `MODEL_IDENTITY.md` | T0 改寫裁掉憲章 §5，⛔ 而引用它的檔沒有人回頭改 |

🔴 **三筆的共同點不是粗心，是「這個書寫形式沒有人在查」。**

### 2. 為什麼查不到（**兩個獨立的洞**）

| 洞 | 內容 |
|---|---|
| **形式** | `REF` 只解析 `` `<檔名>.md` §N `` 長式。⛔ 而框架自己有 **68 處**寫的是短式「憲章 §N」 |
| **範圍** | 本感測器只掃 `governance_globs`。⛔ **`scripts/harness/*.py` 完全不在內**——`§5.11` 那一筆就住在 `.py` 檔頭裡 |

⚠️ **`sensor_reference_integrity` 之所以存在，正是因為「`.py` 檔頭從來沒被掃過」。**
🔴 **⛔ 它補了「檔案引用」，沒補「章節引用」。同一個洞補了一半，而補完的那一半讓它看起來補完了。**

### 3. 改了什麼

1. **新增設定鍵 `section_ref_aliases`**：`{錨定詞: 目標檔案}`。⛔ **不硬編**——`R-21`：白名單不得作為掃描範圍的定義方式，且下游專案的簡稱不會跟這裡一樣。
2. **章節引用檢查的範圍改為 `code_globs ＋ launcher_globs ＋ governance_globs`**（與 `sensor_reference_integrity` 同一套）。
3. **設定了錨定詞卻找不到目標檔 → `ALIAS_TARGET_MISSING`（INCOMPLETE）**，⛔ 不是靜默跳過（`R-33`）。
4. **統計欄新增「檢查的章節引用」**——⛔ 沉默不是通過。

### 4. 🔴 判準第一版太寬，⛔ 而且是它自己抓到的

**擴大範圍後第一次執行，就把 `HANDOFF.md` 報成有兩個 `## 3.` 標題。**
**其中一個是圍籬區塊內、示範「必備五項」長相的那一行**——⛔ **範例文字不是標題。**

⚠️ **這個缺陷原本就在長式檢查裡**，只是 `governance_globs` 內剛好沒有人引用 `HANDOFF.md §3` 才沒發作。
**擴大範圍⛔ 不是製造了它，是讓它現形。**

**修法：剝除圍籬區塊，⛔ 不用豁免清單（`R-20`／`R-21`）**——修正的是「判準被套用在什麼上」。
**代價寫明：一個真的被放進圍籬區塊裡的標題會看不見。⛔ 沒有人會那樣寫。**

### 5. ⚠️ 感測器當場抓到我兩次

**我在新註解裡把長式寫成真檔名當範例**，於是
`[FAIL] DANGLING_FILE_REF: … 引用 `<檔名>.md`，但專案中找不到這個檔案`——**共四處**。
🔴 **家族「描述缺陷時實例化它」再一次，⛔ 而且發生在我正在寫的那一段解釋裡。**
**已全部改為角括號佔位形式。**

### 6. 成對樣本（四組，⛔ 兩組是「不得誤報」那一半）

| fixture | 期望 |
|---|---|
| `secref_alias_bad/` | 短式懸空 → **WARN** |
| `secref_alias_ok/` | 短式可解析 → ⛔ **不得誤報** |
| `secref_py/` | 引用住在 `.py` 檔頭 → **WARN**（證明範圍已擴及程式碼） |
| `secref_fence/` | 圍籬區塊內的假標題 → ⛔ **不得誤報**（本次修正的成對樣本） |

**自測 50 → 54 項。兩版皆 `run_all_sensors.py` exit 0、`run_selftest.py` 54／54。**

### 7. ⛔ 本則不宣稱

- ⛔ **不宣稱其他簡稱也被查了。** 目前只設定了「憲章」一個錨定詞；
  **`R-35`：這是盤點的結果——⚠️ 我沒有盤點框架裡是否還有第三種引用寫法。**
- ⛔ **不宣稱引用指向的內容是對的。** 它只回答「那個章節存在且唯一」。

---

---

## #16 ｜ 2026-08-26 ｜ 空的 `corpus_md/` 被報成「查不了」

### 1. 觸發個案

**框架開始隨附一個空的 `corpus_md/` 資料夾**，讓新使用者看得到提取物要放哪裡。
🔴 **於是每一個新專案第一次執行 `run_all_sensors.py` 都印出 INCOMPLETE。**

⚠️ **⛔ 一個一開始就狼來了的輸出，正是教會人忽略它的方式。**

### 2. 為什麼是判準的問題，⛔ 不是資料夾的問題

**原判準只分兩種狀態：**

```
無目錄            → 不適用，靜默略過
有目錄但無 manifest → INCOMPLETE
```

**⛔ 它漏掉了第三種：有目錄，但裡面還沒有任何提取物。**
**沒有東西要保護，就沒有「保護不了」這回事。**

⚠️ **這與先前一個缺陷同型**：`tool_pdf_to_md.py` 先建好輸出目錄，
才去看有沒有 PDF 要放進去——**同樣是把「不適用」報成「有風險」。**

### 3. 修法

**判準改為看「有沒有提取物」，⛔ 不是看「目錄在不在」：**

| 狀態 | 處置 |
|---|---|
| 無目錄 | 不適用，靜默略過 |
| **有目錄但無提取物** | **WARN `CORPUS_EMPTY`**（新增） |
| 有提取物但無 manifest | INCOMPLETE（不變） |

⛔ **這是結構性修正，不是加豁免清單（`R-20`／`R-21`）。**
⚠️ **`CORPUS_EMPTY` 選 WARN 而不是靜默**：使用者確實還沒有任何東西被查過，
**而「沒有查過」值得說出來，只是它不該讓整批檢查變成 INCOMPLETE。**

### 4. 順帶：說明檔為什麼是 `.txt`

**`corpus_md/` 裡的每一個 `.md` 都會被當成「從 PDF 抽出來的原文」。**
⛔ **所以放一份 `.md` 說明在那裡，它會被報成沒有登記的抽出物。**
**隨附的說明檔因此存成 `.txt`，⛔ 並在檔內寫明理由。**

### 5. 成對樣本

| fixture | 期望 |
|---|---|
| `corpus_empty/` | 空的提取資料夾 → **WARN**，⛔ **不得出現 `CORPUS_MANIFEST_MISSING`** |
| `corpus_unmanifested/` | 有提取物但無 manifest → **INCOMPLETE**（證明舊行為沒有被改掉） |

**自測 54 → 56 項。**

---

## #17 ｜ 2026-08-27 ｜ 🔴 升級會刪掉專案累積的規則——**而說明書正在鼓勵使用者去累積**

**觸發個案（⛔ 不是讀程式碼推論的，是實測）：**
在本機建一個乾淨專案，往 `governance/RULES.md` 加一條 `R-36`、往 `Incident_Log.md`
加一個本專案家族，然後跑 `upgrade.py apply governance`：

| | 升級前 | 升級後 |
|---|---|---|
| `R-36` | 1 | 🔴 **0** |
| 本專案家族 | 1 | 🔴 **0** |

**機制：`upgrade.py` 對資料夾是 `shutil.rmtree` ＋ `copytree`。**

🔴 **真正嚴重的地方：這題框架已經解過一次，而且只解了一半。**
`governance/Incident_Log.md` §1 逐字寫著它為什麼把專案事故搬去 `MY_INCIDENTS.md`：
「框架檔案要能整包升級覆蓋，⛔ 而覆蓋一個裝著你的事故紀錄的檔案，會把它們刪掉。」
**同一個資料夾裡的 `RULES.md` ⛔ 沒有做這件事。**

⚠️ **而 `PROFILE_solo.md` §5.4 寫著「用一年之後，你的 `governance/RULES.md` 會和別人的不一樣，
那正是目的」，下一段接著列出「升級不會蓋掉」的東西——⛔ 而清單裡偏偏沒有 `RULES.md`。**
**每個字都是真的，合起來會把讀者帶到相反的結論。**

**修了什麼：** 新增 `my/MY_RULES.md`（`P-xx` 編號，框架規則逐字複製在 §1）、
`sensor_my_rules.py`、`tool_sync_my_rules.py`；憲章新增 §6.4「一份文件只能有一個主人」。

**成對樣本：** `run_selftest.py` 的 `my_rules_case()` 六項。

⚠️ **本感測器上線後抓到的第一筆，是它自己的解析缺陷**——`MY_RULES.md` §1 結尾的插入點
註解被算進最後一條規則的正文。**第二筆更值得記：首版的覆寫判準是「標記那一行後面有字」，
於是 `**R-19** [本專案覆寫] <原本的條文>` 會通過——⛔ 因為那些字是條文本身，不是理由。
判準已改成「標記必須自成一行」，並補了一項回歸測試。**

---

## #18 ｜ 2026-08-27 ｜ 🔴 `deny` 在單人專案裡從來沒有生效過，而單人是預設情境

**觸發個案：** `sensor_scope_and_t0.py` 舊版在 `write_scopes` 為空時整段跳過 ②，
**⛔ 連 `deny` 一起跳過。而 `PROFILE_solo.md` §6 要求單人專案把它留空。**

🔴 **後果：憲章 §6.3 的「機械對應物是 `deny`，清空它就是完整授權」，
在單人專案裡清空前後完全一樣**——**那句話描述的是一個沒有在運作的機制。**

⚠️ **同一輪的第二件事：`t0_docs` 被程式無條件併進 `denied`，於是設定關不掉 T0 的保護，
而 `framework_config.py` 的註解同時寫著「T0 刻意不列在這裡，治理 Agent 可以維護它們」。**
🔴 **同一個人寫的兩段註解，意圖相反：`deny` 裡「沒有 T0」是一個空缺，而空缺本身不帶意圖。**

**修了什麼：**
① T0 兩份逐字列進 `deny` 的預設值，程式端⛔ 不再追加任何東西。
② 權限檢查⛔ 不再取決於 `write_scopes`；單人情境改為 WARN ＋ 列名
（`DENIED_PATH_TOUCHED_UNATTRIBUTED`）。
⚠️ **⛔ 不是 FAIL：主持人本人改台帳是正常的（`R-19`）；⛔ 也不是靜默（`R-22`）。**
③ `governance_config.json` 的未知鍵改為 FAIL ＋ `difflib` 最接近鍵名建議。

**成對樣本：** `scope_case()` 四項（deny 含／不含 T0、單人改台帳／改一般檔）
＋ `config_key_case()` 三項。

⚠️ **`config_key_case()` 的判準被自測當場修正一次：首版以退出碼判定，
而樣本目錄沒有 git repo，合法設定那兩項會得到 exit 2（`SCOPE_UNCHECKABLE`）——
⛔ 那是對的行為。判準改為「輸出有沒有報未知鍵」。**

**自測 57 → 69 項。**

---

## #19 ｜ 2026-08-27 ｜ 🔴 一段從 v1.0.0 起就壞掉、而從來沒有執行過的程式碼

**觸發個案：** 主持人在自己的機器（繁體中文 Windows）跑 `run_all_sensors.py`，
`sensor_scope_and_t0.py` **兩版都崩潰**：

```
AttributeError: 'NoneType' object has no attribute 'strip'
  top.stdout.strip()
```

**`subprocess.run(..., capture_output=True, text=True)` 回 `returncode == 0`，
⛔ 而 `stdout` 是 `None`。**

🔴 **這個缺陷從 v1.0.0 起就在。它到 v1.4.1 才第一次現形，
⛔ 因為舊版在 `write_scopes` 為空時整段跳過 ②——而 `PROFILE_solo.md` 要求單人專案留空。**
**⇒ 那段程式碼在任何真實使用者的機器上從來沒有執行過。**
⚠️ **`#18`（讓 `deny` 在單人專案生效）⛔ 不是造成崩潰的原因，它是讓它現形的原因。**

⚠️ **框架的處置是對的：`run_all_sensors.py` 把崩潰讀成 `SENSOR_CRASHED` → INCOMPLETE，
總結是 INCOMPLETE ⛔ 而不是 PASS，也⛔ 不是 FAIL。`R-22` 當場生效。**

**修了什麼（兩件）：**

**① `stdout` 不是字串時，判 INCOMPLETE 並把事實印出來（型別、stderr）。**
⛔ **⚠️ 這裡刻意⛔ 不宣稱知道原因**——依 `R-34`，能力邊界的權威源是量測，⛔ 不是推測。
🔴 **同時擋掉一個更糟的失敗：往下走的話 `pathlib.Path("")` 會變成當前目錄，
於是它會回報「專案位於另一個 repo 之內」——一個讀起來合理、而且是錯的診斷。**

**② 專案位於 repo 子目錄時，⛔ 不再拒絕回報，改為把範圍限縮到該子樹。**
⚠️ **舊版一律判 INCOMPLETE。而本框架自己的倉庫就是這個形狀（兩個版本各是一個子目錄），
使用者把專案放進既有的筆記 repo 裡也是——⛔ 那會是一盞永遠亮著的燈（`R-19`）。**
⚠️ **`git status --porcelain` 的路徑相對於 repo 根目錄，⛔ 不是相對於 `-C` 指的目錄——
前綴要自己去掉，⛔ 不能假設。**

**成對樣本：**

| 樣本 | 必須 |
|---|---|
| `git_blind_case()`：PATH 上放一支 exit 0 且不輸出的假 `git` | INCOMPLETE，⛔ 不崩潰、⛔ 不誤診成「另一個 repo」 |
| `subrepo_case()`：專案是 repo 的子目錄，子樹內外各動一筆 | **看得到子樹內的 deny 變更，⛔ 看不到子樹外的檔案** |

🔴 **`subrepo_case()` 的第二半是重點：⛔ 只做第一半就是「對著錯誤的 repo 下判定」，
而那正是舊版拒絕回報所要擋的東西。⛔ 放寬處置的同時，要保住它原本擋的那件事。**

**自測 69 → 71 項。**

### 🔴 追記（2026-08-27，主持人跑了診斷之後）：**原因已確定，⛔ 不再是「未明」**

```
python -c "...capture_output=True, text=True..."
→ Exception in thread Thread-1 (_readerthread):
  UnicodeDecodeError: 'cp950' codec can't decode byte 0x94 in position 16
→ 0 None ''
```

**Python 3.14.2、繁體中文 Windows。`text=True` 而⛔ 沒有指定 `encoding`，
Python 就用系統地區編碼（`cp950`）解 git 的 UTF-8 輸出。**
**路徑裡的 `輔` 在 UTF-8 是 `E8 BC 94`——⛔ 第 16 個位元組 `0x94`，cp950 解不了。**

🔴 **⛔ 最值得記的不是它會失敗，是它失敗的方式：**
**解碼發生在 `subprocess` 的讀取執行緒。⚠️ 那個執行緒死掉，例外⛔ 不會傳到主執行緒，
`communicate()` 回 `None`——於是呼叫端看到的是「exit 0，而且沒有輸出」。**
**⛔ 標準函式庫本身把一個錯誤變成了一個「安靜的空值」。**

### ⚠️ 這是同一個修法只做了一半

**`checkpoint.py` 與 `review_changes.py` 的 `run()` 逐字寫著
`encoding="utf-8", errors="replace"`——⛔ 而 `sensor_scope_and_t0.py` 沒有。**
🔴 **修法早就存在於同一個資料夾裡的兩支程式中，只是沒有被套到第三支。**

⚠️ **也是 `R-34` 的形狀：`_common._force_utf8()` 修的是「我們自己印出去的東西」，
⛔ 而它被讀成「編碼問題都處理過了」。⛔ 一個為真的修正，效力範圍不涵蓋另一條通道。**
**已在 `_force_utf8` 的說明裡補上它⛔ 不涵蓋什麼。**

### 追加的成對樣本

**`subprocess_encoding_case()`：以 `ast` 掃 `scripts/harness/*.py`，
凡 `subprocess.run` 帶 `text=`／`universal_newlines=` 而⛔ 沒帶 `encoding=` 者，一律 FAIL 並指名 `檔名:行號`。**

⚠️ **⛔ 這是靜態檢查，⛔ 不執行任何東西。理由：這個缺陷只在非 UTF-8 地區的機器上發生，
🔴 而一個「在我的機器上測不出來」的判準等於沒有判準。**

**實測：把 `encoding` 拿掉之後，自測當場報
`❌ 有 subprocess.run 會用系統地區編碼解碼：sensor_scope_and_t0.py:83`。**

**自測 71 → 72 項。**


---

## #20 ｜ 2026-08-27 ｜ 🔴 框架取代掉了使用者原本就有的索引

**觸發個案（主持人提供）：某專案套用 v1.3.0 之後，就不再維護自己的檔案索引了——
它的 `file_index.md` 裡⛔ 沒有一條跟研究有關。**

**機制：`file_index.md` 原本是先行專案用來管「研究計畫版本與輔助文件」的表。
在精煉為框架時，它的內容全部變成框架自己的東西，⛔ 而名字沒有換。**
🔴 **於是新使用者看到一份叫「檔案索引」的檔，以為那就是自己的索引——
然後他要嘛把自己的文件登記進去（升級時消失），要嘛就不再登記（實際發生的）。**

⚠️ **同一個專案的交接封包裡還有一句佐證：他刻意⛔ 不把自己寫的工具登記進 `file_index.md`，
理由是「升級時會被覆蓋，登記進去等於把指標放在一個會消失的地方」。**
**⇒ ⛔ 框架不只是忽略了使用者的索引，它取代掉了一個本來存在的機制。**

**新增：`tool_my_index.py`（產生器）＋ `sensor_my_index.py`（守望者）。**

### 三個設計判斷

**① 判準是「框架⛔ 不擁有的每一樣東西」，⛔ 不是一份要收哪些的清單。**
🔴 **一份要收哪些的清單就是白名單（`R-21`）：使用者永遠會發明新的資料夾。**
⚠️ **先行專案的產生器就是那個形狀，而它的作者自己寫下了那句話：
「手寫目錄會漏檔案，⛔ 而產生器會漏目錄。」**
**框架擁有哪些名字，唯一定義處是 `upgrade.py` 的可替換清單——⛔ 本工具 `import` 它，不另寫一份。**

**② 檔案清單由程式產生，說明由 AI 維護，兩者分開存放。**
⚠️ **主持人的實務：研究索引的說明一直是由研究 AI 維護的。
⛔ 而程式對 `.docx`／`.pdf`／`.xlsx` 取不到任何有意義的標題。**
🔴 **沒有說明的檔案⛔ 不會被藏起來，它們有自己的一段**——
**一份看起來完整而漏掉一半檔案的索引，比沒有索引更糟。**

**③ 感測器⛔ 不重寫產生邏輯，它呼叫 `tool_my_index.render()` 再逐字比對。**
⚠️ **兩份產生邏輯只要有一處不同，「是否過期」就會永遠說過期，
⛔ 而真正的原因是兩支程式不一樣。**

### 🔴 這支工具第一次執行就抓到另一個缺陷

**它列出「框架不擁有的東西」，而 `docs/`（兩張說明圖）與六個啟動器按鈕出現在那份清單裡。**
🔴 **⇒ 它們一直是框架的，⛔ 卻不在 `upgrade.py` 的可替換清單上——
意思是：v1.3.0 修好的七處說明圖跑版，⛔ 送不到任何一個既有專案。**
**已把 `docs` 與六個啟動器補進可替換清單。**

⚠️ **`.gitignore` 與 `.gitattributes` 刻意⛔ 不補**——它們是混合所有權（框架給預設值、
使用者往下加），依憲章 §6.4 整包替換會刪掉使用者加的那幾行。
**⛔ 已知代價已寫在 `upgrade.py` 的註解裡。**

### 成對樣本（**六項**）

| 樣本 | 必須 |
|---|---|
| 索引還沒產生過 | **INCOMPLETE**，⛔ 不是 PASS |
| 剛產生的索引 | ⛔ 不得誤報 |
| 🔴 **使用者自己開一個 `deepresearch/`** | **那個檔必須出現在索引裡**（⛔ 證明不是白名單） |
| 產生之後又多了一個檔 | **FAIL `MY_INDEX_STALE`** |
| 說明指向不存在的檔案 | **FAIL `INDEX_NOTE_DANGLING`** |
| 說明檔壞掉 | **INCOMPLETE**，⛔ 不得當成「沒有說明」 |

⚠️ **第三項的判準被自測當場改過一次：首版查的是「感測器的輸出」，
⛔ 而感測器只印統計、不印檔案清單。判準改為查產生出來的索引檔本身。**
🔴 **⛔ 那是我把判準對錯了對象，⚠️ 而自測抓到了它。**

**自測 72 → 78 項。感測器 8 → 9 支。**

---

## #21 ｜ 2026-08-28 ｜ 🔴 一支感測器報的是「你的作業系統不一樣」

**觸發個案：v1.4.1 發布當下。主持人跑步驟 2，`sensor_my_index.py` 報 `MY_INDEX_STALE`。
他手動跑了一次 `tool_my_index.py`，就變綠了。**

**⚠️ 主持人的回報值得逐字記下來：「我不知道這樣會不會有問題，所以還是跟你說一聲。」**
🔴 **那句話正是這一則存在的理由——他被一個假警報逼著在發布途中做了一個他不確定的動作。**

### 原因

**`tool_my_index.py` 舊版寫的是 `sorted(root.rglob("*"))`——⛔ 排的是 `Path` 物件。**
**⚠️ `WindowsPath` 的比較會先 casefold，`PosixPath` ⛔ 不會。**

| | Linux | Windows |
|---|---|---|
| `PROJECT.md` vs `corpus/` | `P`(0x50) < `c`(0x63) → **前面** | casefold 後 `project` > `corpus` → **後面** |

🔴 **我在 Linux 產生索引、隨 v1.4.1 一起發布，於是它在 Windows 上必然報過期。**
**⛔ 它報的⛔ 不是「索引過期」，是「你的作業系統跟產生它的那台不一樣」。**

⚠️ **這是 `R-19` 的教科書形態：一個對正確狀態報警的判準。**
**⛔ 而它的實際後果已經發生了——主持人繞過它，然後不確定自己有沒有做錯事。**

### 修了什麼

**排序的鍵改成「相對路徑的 posix 字串」。⛔ 不排 `Path` 物件。**
**Python 的 `str` 排序在兩個平台上都是碼點順序，⛔ 沒有平台差異。**

### 成對樣本

**`index_order_case()`：樣本裡放 `Zed.md` 與 `apple/x.md`
（`Z` 0x5A < `a` 0x61，⛔ 而 casefold 後 `apple` 在前），
斷言索引裡 `Zed.md` 出現在 `apple/x.md` 之前。**

⚠️ **⛔ 要誠實說它的效力範圍：這一項只在「檔名不分大小寫」的平台上會亮。**
**⛔ 在 Linux 上，舊寫法與新寫法的結果相同，所以它抓不到。**
🔴 **保留它的理由是：發布程序的第一步就在 Windows 上跑自測——
⚠️ 那正是這個缺陷唯一會現形的地方。**

⚠️ **⛔ 本框架不宣稱已經掃過同一形狀的其他地方**——
**「對一個排序行為跟平台有關的型別呼叫 `sorted()`」⛔ 沒有被盤點過。**

**自測 78 → 79 項。**
## #22 ｜ 2026-09-01 ｜ 根層 glob 越到父目錄，且把外平台啟動器誤當成必需

**觸發個案：** 專案 D 在 Windows 上沒有 `.command`，本來是合法部署；但 `_glob_dir()`
處理根層 `*.command` 時把掃描目錄算成專案的父目錄。只要相鄰專案有 `.command`，
引用完整性感測器就會製造 `COVERAGE_COLLAPSE`，把「外平台檔可省略」誤報成「查不了」。

**修正：** 空的 glob 前綴現在明確解析為專案根目錄；`active_launcher_globs()` 只要求
本平台啟動器存在，但公版或專案若實際帶有外平台啟動器，仍一併掃描。引用完整性與
治理文本兩支感測器共用同一判準。

**成對樣本：** `run_selftest.py` 的 `root_glob_platform_case()`：相鄰 `.command` 不得污染
根層 glob；Windows 缺 `.command` 與 macOS 缺 `.bat` 都不得誤報。原生 macOS 啟動執行
仍待 macOS 環境覆核，這些樣本只驗掃描判準。

---

## #23 ｜ 2026-09-01 ｜ 覆蓋崩潰的第二次掃描忘了排除 `_upgrade/`

**觸發個案：** Claude 的 v1.4.4 覆核指出，`resolve_globs()` 雖然會排除 `_upgrade/`，
但 glob 命中 0 個檔之後，`dead_glob_findings()` 又用未過濾的 `rglob()` 搜一次同副檔名。
所以只要下載包內有 `.txt` 或 `.command`，根層死 glob 就會被誤判成
`COVERAGE_COLLAPSE`。

**修正：** 第二次遞迴檢查現在與第一次展開使用同一份 `excluded_dirs` 判準。

**成對樣本：** `_upgrade/` 內同時放 `.txt` 與 `.command`，兩者都不得製造覆蓋崩潰；
再於非排除的子目錄放一份真 `.txt`，確認原本應報的 `COVERAGE_COLLAPSE` 仍會出現。
本次全套自測由 86 增為 95；其中兩項直接覆蓋本判準，其餘覆蓋同輪的升級與 CLI 修正。

---

## #24 ｜ 2026-09-01 ｜ 路徑排序的第七處，與一個查不到它的盤點

**觸發個案：** v1.4.1 的 `tool_my_index.py` 用 `sorted(root.rglob("*"))` 排 `Path` 物件
產生索引，而 `WindowsPath` 的比較會先 casefold、`PosixPath` 不會。主持人第一次執行就
拿到 `MY_INDEX_STALE`，而他什麼都沒做錯。v1.4.2 修了那一處。

**⚠️ 這一則要記的不是那個修正，是那次盤點本身：** 當時的結論寫成「另有六處只影響
輸出順序」。本輪重新盤點得到的是 **七處**——`framework_config.py::resolve_globs()`
的 `sorted(set(files))` 沒有被算進去，而它決定每一支感測器的掃描順序。
🔴 **一個寫死在散文裡的處數，本身就是會漂的數字（`R-16`），而它漂了。**

**修正：** 七處全部改為 `key=lambda p: p.as_posix()`。⛔ 它們今天仍然不是缺陷——
沒有任何一處的輸出會被存起來再比對。改的理由是「同一個專案在兩台機器上輸出不同」
這件事本身會讓任何未來的比對變得不可靠。

**成對樣本：** `run_selftest.py` 的 `path_sort_case()`。與 `subprocess_encoding_case()`
同型的**靜態** AST 檢查：`sorted(...)` 的第一個引數含 `.glob()`／`.rglob()` 而該次呼叫
沒有 `key=` 即為違例。⚠️ **靜態的理由：這個差異只在 Windows 上顯現，而一個
「在我的機器上測不出來」的判準等於沒有判準。** 成對的另一半直接把 v1.4.1 那一行
原始寫法餵給判準，確認它抓得到——⛔ 一個永遠印綠燈的判準保護不了任何東西。

⛔ **本判準的已知缺口：** 它只認得「就地 glob 再排序」的寫法，
⛔ 不涵蓋先把路徑存進變數、之後才排序的形式。寫在這裡，不留白。

自測由 95 增為 97。

---

## #25 ｜ 2026-09-01 ｜ 第十支：升級「換到一半」不會產生任何錯誤訊息

**觸發個案：** 專案 D 的 `policy/` 停在 v1.3.0，而 `governance/`、`profiles/`、`docs/`
已是 v1.4.2。**該專案的九支感測器全綠、自測全過。**
🔴 **⛔ 沒有任何一支在看「這幾包是不是同一版」，所以那個狀態是靜默的**——
⚠️ **它是靠一次外部稽核才被發現的，⛔ 而稽核不是每個專案都會有。**

**新增：** `sensor_version_consistency.py`（第十支）。
`VERSION_MISMATCH` FAIL（套件之間版本不同）／`VERSION_UNREADABLE` INCOMPLETE
（包在、版本讀不到）／`PACKAGE_ABSENT` WARN（這個專案沒有這一包）。

⚠️ **三個等級是刻意分開的：**「你少了一包」與「你的兩包版本不同」是兩件事，處置也不同；
🔴 **而「讀不到」⛔ 不得混進「一致」——那正是本框架最不能被稀釋的那個狀態。**

⛔ **它不宣稱的事：** 版本字串只回答「這個資料夾自稱哪一版」。
**同一個專案 D 還有另一半：`prompts/_VERSION` 寫 v1.4.2，而 `_COMMON_BLOCKS.md` 的
內容仍是 v1.3.0。⇒ 本支抓得到「自稱不一致」，⛔ 抓不到「自稱一致而內容不同」**——
那件事的規定動作是 `upgrade.py diff`。**寫在這裡，不留白。**

**成對樣本（五項）：** 六包同版不得誤報／`policy` 落後一版必須抓到（＝專案 D 的真實狀態）／
缺一包只能是 WARN／`_VERSION` 讀不到只能是 INCOMPLETE／
🔴 **`framework_config.version_packages` 與 `upgrade.FRAMEWORK_DIRS` 必須是同一組名字**。
⚠️ **最後那一項是「兩份拷貝」的守望者：兩邊不同步時，會出現「升級換得了、
感測器卻不比對」的那一包——⛔ 而那正是 `docs/` 在 v1.3.0～v1.4.0 之間的真實狀態。**

⛔ **另一半刻意不放進 harness：** 根目錄的 `CITATION.cff`、`CHANGELOG.md` 與「兩個語言
版本」這個佈局在下游專案裡不存在。**那一半是 `本地工作區/維護工具/check_release_versions.py`，
一支發版閘門。** ⚠️ **理由：一支在使用者機器上永遠 INCOMPLETE 的感測器，
會教人忽略 INCOMPLETE（`R-19`）。**

感測器 9 → 10 支。自測 97 → 102 項。

---

## #26 ｜ 2026-09-01 ｜ 框架修訂一條既有規則時，既有專案沒有任何規定動作

**觸發個案：** v1.4.4 依裁決 `B-4` 在 `R-34` 補了一行「權威敘述的效力範圍，本身也是
需要被查的東西」。**⚠️ 這是本框架第一次修訂一條已經發布出去的規則。**

🔴 **然後這件事就浮出來了：`my/MY_RULES.md` §1 是逐字副本，`sensor_my_rules.py` 以編號
為鍵比對，⛔ 而 `tool_sync_my_rules.py` 只會「附加缺少的條目」、刻意不覆寫既有條目。**
⇒ **那一行文字會讓每一個既有專案立刻拿到 `RULE_TEXT_DRIFT` FAIL，
⛔ 而唯一的出路是請人手動把新文字貼進去。**
⚠️ **「請人手動貼」正是這支工具存在的理由所要避免的那件事**——
**條款同步感測器曾抓到有人把 `extremely` 打成 `extremely high`。**

**⛔ 舊行為的理由不是錯的：** 「一支會自動覆寫的工具，會把使用者刻意的修改靜靜地抹掉」。
🔴 **但框架自己的判準已經規定：刻意的修改⛔ 必須標 `[本專案覆寫]` 並寫下理由，
否則就是 `RULE_TEXT_DRIFT` FAIL。⇒ 一條沒有標記的條目，其副本是純衍生的。**

**修正：** `tool_sync_my_rules.py` 新增 `--adopt`：把「正文不同、⛔ 而未標覆寫」的條目
換成公版原句，逐條印出「舊的第一行 → 新的第一行」。
⛔ **標了覆寫的一律跳過並逐條印出來。**
⚠️ **`--adopt` 是明確旗標而⛔ 不是預設：一個忘了標覆寫的刻意修改，
在這裡看起來與一筆單純的過期拷貝完全一樣。⇒ 檢查點是它的還原路徑。**

**同輪重構：** 覆寫判準抽成 `sensor_my_rules.is_override()`，感測器與工具讀同一個函式。
🔴 **⛔ 兩份判準只要有一處不同，就會出現「感測器說是覆寫、工具卻把它蓋掉」**——
⚠️ **而那正是本支感測器存在的軸二形狀，發生在它自己身上（憲章 §3.2）。**

**成對樣本（四項）：** 未標覆寫 ＋ `--adopt` 必須採用公版／🔴 **已標覆寫 ＋ `--adopt`
一個字都不得改，且要印出跳過**／沒有 `--adopt` 時⛔ 不寫檔而且⛔ 不沉默／`--adopt` 冪等。

⚠️ **一則值得記的過程：這四個樣本第一次跑，第二與第三項都紅了，⛔ 而工具是對的——
fixture 少放了一條規則，於是「有東西要補」與「有東西漂移」混在一起，
`檔案沒有被改` 這個斷言分不出是哪一個造成的。**
🔴 **修的是 fixture，⛔ 不是把斷言放寬。**

自測 102 → 106 項。

---

## #27 ｜ 2026-09-01 ｜ 一個資料夾退役之後，它在既有專案裡變成什麼

**`policy/` 於 v1.4.4 併入 `governance/`。**

🔴 **⛔ 這裡要記的不是「搬了四個檔案」，是搬完之後那個名字的處境：**
**它從 `FRAMEWORK_DIRS` 上被拿掉了。⇒ 在一個既有專案裡，`policy/` 從此**
**⛔ 沒有任何東西會替換它、⛔ 沒有任何感測器會看它、⛔ 也沒有任何東西會提到它。**
⚠️ **內容永遠停在退役那一版，而它看起來仍然像一個框架資料夾。**

**⛔ 升級工具不刪使用者的檔案（憲章 §6.3），⚠️ 但它必須說出來**——
🔴 **一個沒有人知道的孤兒資料夾，與一個過期的框架文件完全一樣。**

**新增：** `upgrade.py` 的 `RETIRED_DIRS`（`目錄名 → (退役版本, 內容去了哪裡)`）。
`check`、`diff` 與**每一次成功的 `apply`** 都會列出仍然存在的孤兒；
`apply policy` 回一句專屬訊息，⛔ 而不是通用的「這不是可替換項目」。

⚠️ **為什麼 `apply` 之後也要印：** 🔴 **退役提示只存在於新版程式裡，
⛔ 而使用者是用他手上那份舊程式跑 `diff` 的**——
**⇒ 他能看到這則訊息的最早時機，是換完 `scripts` 的那一刻。**
⛔ **只印在 `check`／`diff` 上，等於對「照順序升級」的人靜默一整輪。**

**成對樣本（四項）：** 專案裡還有 `policy/` 時必須列出／🔴 **乾淨的新專案⛔ 不得出現退役提示**
（否則就是常態紅燈）／空的孤兒一樣要列出（判準是「資料夾存在」，⛔ 不是「裡面有東西」）／
🔴 **`RETIRED_DIRS` 與 `FRAMEWORK_DIRS` ⛔ 不得有交集**——**一個名字若同時「可替換」又「已退役」，
判定會取決於程式碼順序。**

**端到端驗證：** 用 `v1.4.2` tag 的真實內容建一個既有專案，種入 `P-01`、`專-①`、
`my/tools/mine.py` 與一份已填的 `TEMPLATE_decompose.txt`，逐包升級。
`prompts` 如預期被 target-only 擋下；搬成 `第一個想法.md` 後成功；
四樣專案自有內容全部存活；`policy/` 被列名而⛔ 未被刪除；最終 10 支感測器 PASS、自測 110／110。

---

## #28 ｜ 2026-09-02 ｜ 三層保護裡只有一層真的存在

**外部覆核（Codex，Windows）對 v1.4.4 提出六項，其中三項是發版阻斷。⛔ 三項都在同一輪的施工裡。**

### 🔴 `F-03`：`--adopt` 的三層緩解，實際只有第一層

**檔頭寫的是「明確旗標 ＋ 寫入前預覽 ＋ 檢查點可還原」。實測：**

| 宣稱 | 事實 |
|---|---|
| 「**換掉之前**印出舊 → 新」 | ⛔ `write_text()` 在兩個 `print` **之前**——**先寫再印** |
| 「檢查點還在」 | ⛔ **無條件字串**；測試用的專案根本沒有 `.git`，而它照樣這樣印 |
| 預覽讓人看得到改了什麼 | 🔴 **`R-34` 的舊／新第一行逐字相同**——變更全在第二行以後 |

⚠️ **第三點最值得記：`R-34` 是本框架第一次修訂既有規則，也就是 `--adopt` 的第一個真實使用情境。**
**⇒ 預覽在那一刻印出兩行一模一樣的字。⛔ 它不只是沒幫上忙，它讓使用者以為「沒改什麼」。**

**修正（主持人裁決 2026-09-02：「由 `--adopt` 自行建立並驗證檢查點；失敗就不寫入」）：**

🔴 **驗證判準⛔ 不是「`checkpoint.py` 退出碼 0」**——**實測它在「沒有變更」時也回 0。**
⚠️ **也⛔ 不逐位元組比對 `git show HEAD:<路徑>`：專案可能在 repo 子目錄（路徑基準不同），
且 Windows 的 `core.autocrlf` 會讓 blob 與工作檔位元組不同——⛔ 那會把健康的專案判成不可還原。**
→ **改用 git 自己的語意：`rev-parse --git-dir` → `rev-parse --verify HEAD`
→ `ls-files --error-unmatch`（⛔ 未追蹤的檔案在 `git diff` 裡看不見）→ `diff --quiet HEAD -- <rel>`。**
**⚠️ 後兩項合起來同時涵蓋「工作樹髒（剛提交）」與「工作樹乾淨（原文本來就在 HEAD）」。**

⚠️ **閘門只管覆蓋型寫入，⛔ 不管純附加**——**裁決逐字只授權 `--adopt`，
⛔ 把它擴大到附加缺少的條目是替主持人加碼（`CM-34`）；而附加不刪任何東西。**

**預覽改為 `difflib` 的逐行比對（裁決 `A4a: A`），每條上限 8 行並印出行數變化。**
🔴 **若判準說有 drift 而逐行比對是空的 ⇒ 非零退出，⛔ 不寫入**——**兩者不可能同時為真。**

### `F-04`：字串排序冒充 SemVer（裁決 `A4b: B`）

**實測 `sorted({'v1.9.0','v1.10.0'})[-1]` → `v1.9.0`。**
⚠️ **感測器仍會正確報 mismatch，⛔ 但訊息會叫人補到比較舊的那一版。**
🔴 **更根本的理由：本感測器看不到 `_upgrade/`，⛔ 所以它沒有能力知道目標版本**——
**「該補到哪一版」的權威源是升級來源（`R-34`）。⇒ 移除 `newest`，改為指向 `upgrade.py diff`。**

### `F-07`：抽出共用判準⛔ 沒有消除分歧，只是換了方向

**`is_override()` 回傳 `(有沒有標記, 理由夠不夠長)`，⛔ 而工具只取 `[0]`。**
⇒ 一條「標了覆寫但沒寫理由」的規則，工具說「已標覆寫」，
🔴 **而 `sensor_my_rules.py` 正在對它報 `OVERRIDE_WITHOUT_REASON` FAIL。**
⚠️ **⛔ 行為不改（有標記就不碰是對的），改的是那句話說了什麼。**

### 成對樣本

**`adopt_gate_case()` 十列**，取代前一輪的 `sync_adopt_case()`：
無 Git／Git 正常／已標覆寫（有理由）／零變更不得建檢查點／沒有 `--adopt` 不寫檔但不沉默／
預覽必須印出實際差異行與行數變化／🔴 **工作樹乾淨必須通過**／🔴 **專案在 repo 子目錄**／
🔴 **檔案未被追蹤必須拒絕**／🔴 **覆寫標記無理由時訊息要說感測器會 FAIL**。
**後四列是本框架自補的，⛔ 不在原提案的矩陣裡。**

**`version_consistency_case()` 增兩列：** `v1.9.0` 與 `v1.10.0` 並存必須報 mismatch，
且訊息⛔ 不得出現任何目標版本。

自測 110 → 120。

---

## #29 ｜ 2026-09-02 ｜ 一支程式替人簽了名，而它從 v1.4.1 起就在做

🔴 **`v1.4.3` 從未發布。** 發布前的覆核在其中找到五項缺陷，三項紅級；主持人裁決退回
`--adopt` 的整個寫入路徑，其餘內容改以 `v1.4.4` 發布。

### 🔴 最嚴重的一項⛔ 不是新引入的

**`upgrade.py` 自 v1.4.1 起以 `checkpoint.py --root <root>` 呼叫，⛔ 沒有傳 `--mode`。**
**而 `checkpoint.py` 的預設是 `human`，human 模式無條件執行 `git tag -f reviewed`
並印「人工檢查點：我看過了」。**

**⇒ 在 `v1.4.2` 的 tag 上實測重現的完整鏈：**

```
① 使用者按過「記錄快照」            reviewed = 15fe2c4
② AI 寫進 ledgers/Claim_Ledger.md 並以 --mode ai 存檔
   → review_changes 顯示那一筆                    ✅ 使用者看得到
③ 使用者升級框架：upgrade.py apply
   → 「人工檢查點：我看過了」「reviewed 基準已移到最新的檢查點」
   → review_changes：⛔ 台帳那一筆消失了
```

⚠️ **後果⛔ 不是「標籤變了」，是「還沒看過」被清空：**
🔴 **一筆 AI 寫進主張台帳、人從未審閱的內容，因為使用者升級了框架，
就被標成「我看過了」並從待審清單消失。**
**⛔ `reviewed` 是本框架「人是唯一裁決者」這個宣稱的唯一機械載體。**

**修正：`checkpoint.py` 新增第三種身分 `tool`（必填 `--tool-id` 與 `--operation`）。**
🔴 **`ai` 與 `tool` 都⛔ 不得移動 `reviewed`，也⛔ 不得印人工審閱的橫幅。**
`upgrade.py` 改為明確傳 `--mode tool --tool-id upgrade --operation apply-<目標>`。

⛔ **為什麼不叫工具走 `ai` 模式：** `ai` 強制要求 `--role` 與具體 `--model`，
**而一支本地工具⛔ 不是 `VALID_ROLES` 的任何一個，也沒有型號**——
⚠️ **要它走 `ai`，就是要它虛構一個型號，⛔ 而 `MODEL_IDENTITY.md` 存在的理由正是禁止那件事。**

⚠️ **已知代價：`--mode` 仍有預設值 `human`。** 改成必填會讓舊專案的 `.bat`／`.command`
立刻壞掉，⇒ 必填留到 v1.5.0 與啟動器同輪改。
⚠️ **另一個已知代價：`checkpoint.py` 是 `git add -A`，⇒ 工具觸發的還原點會把使用者當下
所有未完成的編輯一起提交。** ⛔ 對「還原點」這個用途是對的（整棵樹都拿得回來），
**但它改變了歷史的顆粒度，⚠️ 現在至少訊息會說那是工具做的。**

🔴 **⛔ 已經被移動過的標籤，程式修好也不會自己還原**——`SETUP.md` 新增一節教使用者怎麼查與怎麼重設。

**成對樣本（`reviewed_tag_case`，9 項）：** `tool`／`ai` 各驗「原本沒有 reviewed → 之後仍然沒有」
與「原本有 → 指向完全不變」，各加一項「⛔ 不得印人工審閱橫幅」；human 模式仍然要移動
（⛔ 這個區分是刻意的，不得一起關掉）；`tool` 缺身分參數必須拒絕；
🔴 **`CP-12` 直接檢查 `upgrade.py` 的呼叫字串含 `--mode tool`——⛔ 因為已發布的缺陷在那裡，
不在任何新增的東西裡。**

### `--adopt` 退回

**三個缺陷：先寫檔才印「預覽」、無條件宣稱「檢查點還在」（測試專案根本沒有 `.git`）、
以及在檢查點程式明確回 1 並印「未寫入任何東西」時仍然寫入。**
⚠️ **第三項的根因是我把「⛔ 不只是看退出碼」實作成了「⛔ 不看退出碼」。**

**⇒ 主持人裁決退回整個寫入路徑。⛔ 這一支現在只做兩件事：附加缺少的條目、逐行報告漂移。**
🔴 **保留下來的是差異顯示**——**沒有工具代勞之後，那份差異告訴使用者要手動貼哪幾行，
⚠️ 它比原本更有用。**

**`R-34` 的修訂保留**（主持人：現在專案規模小，趁現在把規則修好）。
⇒ **既有專案會看到一次 `RULE_TEXT_DRIFT`，規定動作是人工貼上，`SETUP.md` 已寫明四步。**

自測 120 → 128。

### `H-006` 覆核退回的六項（Codex，Windows 獨立覆核）

**⚠️ `tool` 模式的核心修正成立（Codex 在 Windows 重跑同一條端對端鏈，`reviewed` 保持不動），
⛔ 而同一輪找到六項，其中三項是發布阻斷。**

#### 🔴 `R-H006-03`　人工檢查點在標籤建不起來時謊報成功

**`git tag -f reviewed` 的退出碼從來沒有被檢查過。**
**Codex 的故障注入：專案裡先有一個 `reviewed/child`，Git 的 ref 命名空間就再也放不下
`reviewed`，`git tag -f` 回 128——⛔ 而本程式印「reviewed 基準已移到最新的檢查點」並 exit 0。**
🔴 **⇒ 使用者按下「我看過了」，畫面說完成，⛔ 而那個標籤根本不存在。**

**修正：** 檢查退出碼，**再把 `reviewed` 讀回來與 `HEAD` 比對**；任一步不成立就 exit 2 並明說
「提交建立了，⛔ 而標籤沒有移動」。
⚠️ **後置條件⛔ 不是「commit 成功」，是「`reviewed` 確實指向現在的 HEAD」。**
**成對樣本 `CP-14`（4 項）：非零退出／⛔ 不得印「基準已移到」／必須說出標籤沒動／提交本身仍在。**

#### 🔴 `R-H006-02`　升級存了還原點，官方復原路徑卻看不到它

**`upgrade.py` 的完成訊息叫使用者用「查看變更」找回被覆蓋的手動改動；
⛔ 而 `review_changes.py` 的基準固定是 `reviewed`，`tool` 模式又刻意不移動 `reviewed`。**
**Codex 的反例：同一個檔案，`git diff reviewed` 0 行、`git diff HEAD` 193 行。**
🔴 **⇒ pre-image 確實還在，⛔ 而官方介面看不到它。這是我這一輪的修正親手造成的回歸。**

**修正（最小、真話優先）：** 覆蓋前讀回 checkpoint 的提交編號當**收據**印出來，並給
`git diff <收據> --` 與 `git checkout <收據> --`；同時明說「查看變更」看不到它、以及為什麼。
🔴 **⛔ 讀不回編號就不覆蓋**——**一個說不出還原點在哪的還原點，不是還原點。**
編號同時寫進 `git-checkpoint.log`。
⚠️ **代價：它把 git 指令交到了「本框架假設不會用 git」的使用者手上。**
**⇒ 是否要做專用 receipt 架構（持久 ref／`--base`／`restore` 命令）屬紅級機制選擇，
已登記待裁決，⛔ 維護者不自行定案。**

**成對樣本：** `CP-15`（3 項，檢查訊息與閘門的存在與順序）＋
`upgrade_target_only_case` 新增 2 項行為樣本——**沒有 git 時整包替換必須 exit 2 且零覆蓋，
補上 git 之後同一個指令必須成功，且畫面上要出現真正的提交編號與取回指令。**

#### 🔴 `R-H006-04`　既有專案的診斷漏掉最常見的那一種

**原本只教使用者看 `reviewed` 是不是指向 `snapshot …`。**
⚠️ **⛔ 而在乾淨工作區升級（最常見的情形）時，舊版升級是把標籤移到一個既有的 `auto:` 提交上。**
🔴 **更糟的是：`auto:` 既是缺陷的結果，也是正常流程的結果**（人在 AI 做完後按一次「記錄快照」，
標籤本來就會落在那裡）——**⛔ 兩者長得一模一樣，⇒ 訊息開頭⛔ 不能當判準。**

**修正：`SETUP.md` 改成三步。** ① 看提交種類（四種情形都列，並明說 `auto:` ⛔ 不是證據）；
② 查 `git-checkpoint.log` 的 `mode=human` 時間與使用者記憶對照，**並寫明 Git ⛔ 沒有替這種標籤
保留 reflog，免得使用者去找一個不存在的東西**；③ 兩步都判不出來時 ⛔ **不猜、不重設**，改為退回
一個確定看過的提交重看。**⛔ 這一段不得用「查看變更」做——那個按鈕的基準正是被懷疑的東西。**

#### `R-H006-05`　旗標退回了，說明沒有跟著退回

**`--adopt` 已從 argparse 移除，⛔ 而兩版 `upgrade.py` 的完成訊息與兩版 `sensor_my_rules.py`
的 docstring 仍然叫使用者去加那個旗標。**
⚠️ **當時的成對樣本 `SYNC-07` 證明的是「程式會拒絕」——⛔ 而那與「沒有人被叫去用它」是兩件事。**

**修正：** 清掉三處現行說明。**新增靜態掃描 `SYNC-08`：** 掃 `SETUP.md`／`README.md`／
`INITIALIZE_PROMPT.md` 與 harness 內所有 `.py`，任何已退回的旗標只要出現在一個沒有 `[已退回]`
標記的段落就 FAIL。⛔ **不用字詞白名單**（「曾經」「原本」……）——**那種判準換個說法就繞過去了。**
⚠️ **掃描自己也有反證樣本：一段沒有標記的假說明必須被抓到，⛔ 否則「掃過了沒事」與「掃描是空的」分不出來。**

#### `R-H006-06`　版本敘述與雙語

**`tool_sync_my_rules.py` 說含 `--adopt` 的那一版「原訂 v1.4.4」——⛔ 錯了，是 `v1.4.3`。**
⚠️ **根因：我在做 v1.4.3 → v1.4.4 的全域取代時，把自己幾分鐘前寫的例外句一起蓋掉了。**

**英文版 `run_selftest.py` 的 `reviewed_tag_case` 整段是中文的機器拷貝**——
🔴 **⛔ 這是我第三次交出機器拷貝的英文測試。⚠️ 測試通過⛔ 不等於英文介面完成。**
**修正一律是把英文重寫，⛔ 從來不是放寬判定。**

#### `R-H006-01`　裁決編號碰撞（維護端流程，⛔ 不影響本倉庫的任何檔案）

**主持人打 `A4a`、維護者登記 `A-4-a`，⇒ 獨立覆核者搜遍所有紀錄後判定「裁決來源無法追溯」，
⛔ 而裁決其實就在同一份檔案裡。** ⚠️ **一個編號兩個所指，⛔ 而永久紀錄不能靠記憶對上。**
**修正在維護端：裁決編號改為永久唯一、append-only，⛔ 不再沿用會被重寫的小節編號。**

自測 128 → 139。

## #30 ｜ 2026-09-02 ｜ 只有提交編號，卻沒有證明提交裡真的有那個檔案

**觸發個案（Codex，Windows）：** 一個與新版同路徑的既有框架檔被 Git 排除後再手動改寫。
檢查點 exit 0、`HEAD` 也存在，但那個提交的 tree 沒有此檔。`upgrade.py apply docs` 仍然替換，
並宣稱整個專案已保存。實測：`UPGRADE_EXIT=0`、`RECEIPT_HAS_HAND_EDIT_PATH=False`、
`MANUAL_MARKER_SURVIVES=False`。

🔴 **根因：** 程式只證明「有一個 checkpoint commit」。ignore 規則、
`assume-unchanged` 與 `skip-worktree` 都可能讓該提交不同於即將被刪除的工作檔。
**沒有證明 pre-image 已在提交裡，提交編號就不是還原收據。**

**依主持人裁決 `D-20260902-06` 修正：** v1.4.4 完成升級專用 receipt 路徑。

- 替換前逐一驗證每個即將被新版同路徑覆蓋的既有非暫存檔：必須受追蹤、在 checkpoint
  tree 與 index 中是一般 blob，並透過 Git 自己的 attributes／EOL filters 算出相同雜湊。
  任一路徑缺少或不符，就列名、exit 2、零替換。
- 收據 manifest 記錄操作、目標、checkpoint、原 `reviewed`、每個保存路徑／blob／mode，
  以及精確暫存例外。權威副本嵌入 annotated Git object，固定於
  `refs/spark2groundwork/restore/<收據>`；可讀 JSON 鏡像放在
  `git rev-parse --git-path spark2groundwork/receipts` 回傳的位置。ref 同時固定 checkpoint
  與 manifest；JSON 鏡像被改寫會被抓到。
- `upgrade.py receipts`、`receipt-diff <收據|latest> [路徑]`、
  `restore <收據|latest> <路徑>` 讓使用者不必輸入原始 Git 指令。restore 一次只處理一個
  目前存在的一般檔案，先建立並驗證反向收據，只寫 working tree，且永不移動 `reviewed`。
- 下載的 v1.4.4 升級器若以舊專案為目標，`apply` 會使用同一下載包內的 checkpoint 程式；
  否則新 receipt 契約會接到不懂 `tool` 身分的舊 checkpoint CLI，連 `scripts/` 都無法 bootstrap。
- v1.4.4 不自動刪除收據；跨工具共用與清理政策留到 v1.5.0。保證的是 Git 還原語意，
  ⛔ 不是原始位元組。

**成對樣本 `UPG-REC-03`～`UPG-REC-12`：** ignored 內容、兩種 index 隱藏旗標、
持久 ref／manifest 列表、正常 tracked 手改、diff／restore 與反向收據、`reviewed` 不動、
路徑逃逸、目前檔案缺少時拒絕、CRLF 正規化、精確暫存範圍、目標端獨有空目錄與 manifest
竄改，以及下載版 checkpoint bootstrap。**自測 139 → 149。**

### `A-20260902-11`／裁決 48／裁決 54–57（Claude 主筆，2026-09-02）

#### 🔴 行尾與雜湊：三個各自正確的元件，合起來保證產生一個假 FAIL

| 元件 | 各自對不對 |
|---|---|
| `tool_pdf_to_md.py` 用 `write_text(..., encoding="utf-8")` 寫提取物 | ⚠️ **Windows 上寫出 CRLF**，而 `md_sha256` 記的就是 CRLF 的雜湊 |
| `.gitattributes` 的 `*.md text eol=lf` | ✅ 正確 |
| `sensor_claim_ledger.py` 每輪重算雜湊，不符即 FAIL | ✅ 正確 |

🔴 **⇒ 下一次乾淨簽出（clone 到第二台機器／`git checkout`／照 `SETUP.md` §9 復原）
工作區變成 LF，每一個檔案的雜湊都對不上 → `CORPUS_MD_MODIFIED` 整片 FAIL，
⛔ 而內容一個字都沒有被動過。**
⚠️ **失效方向是假陽性，正是 `R-19` 說「會教人忽略感測器」的那一種。**
⛔ **這個缺陷在 v1.4.1 與 v1.4.2 裡現在就在生效**——**⚠️ 它至今沒有發作，
只是因為專案 D 那批提取物是在 Linux 產生後直接寫進去的。**

**修正：** 兩版 `tool_pdf_to_md.py` 的提取物與 manifest 寫入各補 `newline="\n"`。
⚠️ **已產生過的專案要重跑一次提取工具**（manifest 與檔案會一起變成 LF，⇒ 自洽）。

**成對樣本（三項，靜態檢查）：** 判準刻意是個**過近似**——
**一個模組只要會算雜湊（`import hashlib`），它的每一個 `write_text` 都要指定 `newline=`。**
① 掃全 harness 並印分母；② **判準本身要抓得到一個植入的違規**（⛔ 否則它只是永遠印綠燈）；
③ 🔴 **⛔ 不算雜湊的模組⛔ 不得被誤報**——⚠️ **否則下一個人會被逼著在幾十個 fixture 寫入點
補 `newline=`，而那種「為了消警報而加的參數」會把判準本身變成噪音。**

⛔ **已知缺口，寫在這裡不留白：若「寫檔的模組」與「算雜湊的模組」是兩支，本判準抓不到。**
⚠️ **目前框架內沒有那種切分，⛔ 而那是現況，不是保證。**

#### 裁決 48（B-3）：把一個一直都在、⛔ 而看不見的保護印出來

**`sensor_claim_ledger.py` 每輪都在逐檔重算 `md_sha256`，⛔ 而統計欄是空的 `{}`。**
🔴 **⇒ 兩個不同的人、隔了七天，得到同一個結論：v1.3.0 的稽核者找不到「哪一支感測器在比對
manifest」；2026-09-02 專案 D 的使用者又寫了一次「我至今找不到」。⛔ 而它一直都在那裡。**

**修正：** 統計欄新增「逐檔比對雜湊：N 份」。
⚠️ **語料庫是空的時候印「0 份」，⛔ 不是省略**——`R-35`：「無」必須是盤點的結果。

#### 裁決 54–57：失效家族 ⑨、⑩ 與 ① 的兩個新變體

| | 內容 |
|---|---|
| **①⑷** | **量了一個代理量，然後把它當成事實**——⚠️ **代理量本身通常是對的，⛔ 它只是在回答另一個問題**，⇒ 它不會報錯也不看起來可疑 |
| **①⑸** | **把一句在某個範圍內為真的敘述讀成全稱句**（⚠️ 與 `R-34` 本版新增的那一句同源） |
| **⑨** | **批次編輯成功了，⛔ 而它做的不是我要的事。** 🔴 **⛔ 讀 diff 抓不到它**——diff 只說「這一段被加上了」，⛔ 不說「它不該被加」 |
| **⑩** | **為一個環境調好的常數，原封不動搬到另一個。** 🔴 **兩邊逐字相同 ⇒ 任何「兩版是否一致」的檢查都會說一致——「一致」正是它的偽裝** |

⚠️ **⑨ 與 ⑩ 都標 `[框架自身]`：它們發生在維護這套框架的過程中，⛔ 不是別人的事故。**

自測 149 → 152。

#### 裁決 `D-20260902-C25`：家族 ⑪ 與四個具體個案

**⑪ 兩個寫入者共用一個編號空間，⛔ 而配號在各自那一端看起來都成功。**
🔴 **⛔ 與家族 ② 不同：這不是同一項事實被複製成多份紀錄，而是兩個新的東西搶同一個唯一名稱。**
⚠️ **實測同一天三次。⛔ 而「配號前先重讀一次」⛔ 不足以擋它——三次都重讀過，三次都撞到。**
**⇒ 對策是把號碼空間依配號者切開。**

🔴 **它對下游研究專案同樣成立：`Claim_Ledger.md` 的 `M-xx` 與 `Conjecture_Ledger.md` 的 `C-xx`
都是多個 agent 併行追加的編號空間**——⚠️ **兩個 agent 同一輪各配一個 `M-36`，⛔ 目前沒有任何東西會發現。**

**§2.2 新增四個個案（皆為既有家族的樣本）：** 說明編號規則時舉例而例子與真編號分不出來（⑧）；
舉例說明數字會漂的那一句自己漂了（②⑶）；用工具輸出的一個欄位回答另一個問題（①⑷）；
「它被擋下來了」被讀成「有一道檢查」（①⑷）。

**§3 參與者弱點的「AI（生成方）」一列新增：把「自己為了產生建議而訂的判準」當成主持人的義務。**

---

### `D-20260902-X12`／`-X13`：restore peer 與語言版本閘門（Codex 主筆）

- `apply` 與 `restore` 共用相容 peer 選擇：先找執行中 `upgrade.py` 同目錄的
  `checkpoint.py`，再找專案內版本。候選檔只讀精確 tool API 標記、不執行；兩者都不相符時
  零寫入並要求重新下載同語言、同版本的完整套件。
- 收據承諾限縮為「專案本地持久」：私有 ref 在同一 Git 倉庫內固定物件，⛔ 一般 clone 或
  只備份工作檔案不會自動帶走。
- `apply` 在任何檢查點、收據或替換之前比較 edition fingerprint。中英文各自的三個既有啟動器
  可形成 Windows-only、macOS-only 或雙平台簽名；跨語言、混合或缺漏一律拒絕。正式 edition
  欄位留到 v1.5.0。
- 🔴 **閘門被擋下時會指名它實際看到的啟動器。** **觸發個案（發布前覆核，2026-09-03）：**
  判定只讀那十二個框架檔名，⛔ 不看是誰放的 ⇒ **一個合法的中文專案，只要自己有一個叫
  `snapshot.bat` 的檔案，就會被判「混合」並整次拒絕**（實測 `(None, 'mixed')`）。
  ⛔ **原訊息只說「無法證明是同一語言版本」並要求重新下載套件**——⚠️ **而問題不在下載包，
  ⇒ 使用者會一直重下載，而原因永遠不會出現。** 現在訊息逐一列出兩側看到的檔名與所屬版本。
  ⚠️ **⛔ 判定邏輯一個字都沒有改**：新的盤點函式只在失敗路徑被呼叫，
  🔴 **⇒「怎麼說明」與「怎麼決定」分開，改前者不會動到後者。**
  ⚠️ **原本那句「自建的其他 `.bat`／`.command` 不參與判定」是無限定敘述而實際有範圍**
  （`R-34` 的新增條），⇒ 感測器說明、自測訊息與發行說明三處都已改成實際範圍。

**新增六項自測：** 六種合法簽名、**名字不同的**無關啟動器、四種不確定狀態零寫入、
bootstrap 後 restore 與反向收據、找不到相符 peer 時不執行舊程式且零寫入、
**撞名被擋下時訊息必須指名那個檔案**、**在排除清單內而確實存在的檔案⛔ 不得被說成不存在**。
**自測 152 → 159。**

- 🔴 **索引工具把「在排除範圍內的檔案」說成「檔案不存在」。**
  **觸發個案（專案 D，2026-09-02）：** `archive/` 在 `my_index_exclude` 內，
  ⇒ 一個真的存在的退役檔被 `INDEX_NOTE_DANGLING` 印成「檔案被搬走或改名了」。
  🔴 **⛔ 而照那句話去做（刪掉那筆說明）⛔ 不會出錯 ⇒ ⛔ 沒有人會發現它在說謊。**
  ⚠️ 舊寫法 `dangling = sorted(k for k in notes if k not in set(files))` 只問「有沒有被掃到」，
  ⛔ **從來沒有去看過磁碟。** 現在判「不存在」之前先 `exists()`，兩種情形分成兩個代碼：
  真的不見了 → `INDEX_NOTE_DANGLING`（FAIL）；**檔案在、只是被排除 → `INDEX_NOTE_EXCLUDED`（WARN）。**
  ⚠️ **⛔ 沒有把 `archive/` 移出排除清單**——**用擴大掃描範圍去修一個訊息錯誤，
  是把成本加在錯的地方**（提案人自己也這麼建議）。
- **憲章 §6.1 的一句話與同節第 2 條通則、以及 §6.3 直接矛盾。**
  舊寫法：「⛔ **無論哪一種情境**，`ledgers/` 一律不在任何 AI 的產出區內」；
  ⚠️ **而九行之後就寫著「這是預設值，⛔ 不是禁令。見 §6.3」，§6.3 又寫著
  「使用者可以授權 AI 寫入任何東西，機械對應物是 `deny`，清空即完整授權」。**
  🔴 **三句話在同一份 T0 裡。** 已改成「預設值 ＋ 以 `deny` 為準」，並寫下觸發個案。
  ⚠️ **⛔ 未解決的另一半：「本專案對某條框架條文的裁決」⛔ 沒有不被升級覆蓋的存放處 ⇒ v1.5.0。**

---
